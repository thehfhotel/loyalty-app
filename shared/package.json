{
  "name": "@hotel-loyalty/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for Hotel Loyalty App",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.22.4",
    "date-fns": "^2.30.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/node": "^20.10.4"
  },
  "files": [
    "dist",
    "src"
  ]
}