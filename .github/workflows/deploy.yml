name: Optimized CI/CD Pipeline with Security & Testing

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]
  workflow_dispatch:

# Workflow-level concurrency: prevent parallel runs on same branch
# This prevents race conditions when multiple commits push in quick succession
concurrency:
  group: "ci-${{ github.ref }}"
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

# SECURITY: Top-level permissions set to minimal read-only
# Write permissions are granted at job-level only where needed (principle of least privilege)
permissions:
  contents: read
  actions: read
  pull-requests: read

env:
  NODE_VERSION: 24
  CACHE_VERSION: v3

jobs:
  # =============================================================================
  # PHASE 0: SHARED WORKSPACE PREPARATION (NEW - 30-60 seconds)
  # =============================================================================

  prepare-workspace:
    name: "🚀 Workspace Preparation"
    runs-on: self-hosted
    timeout-minutes: 2
    outputs:
      workspace-ready: ${{ steps.setup-complete.outputs.ready }}
      backend-cache-hit: ${{ steps.backend-cache.outputs.cache-hit }}
      frontend-cache-hit: ${{ steps.frontend-cache.outputs.cache-hit }}

    steps:
      - name: "📥 Checkout code (shared for all jobs)"
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6
        with:
          fetch-depth: 1
          clean: true

      - name: "⚡ Setup Node.js"
        uses: actions/setup-node@395ad3262231945c25e8478fd5baf05154b1d79f # v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: "🗂️ Prepare directories"
        run: |
          # Verify Node.js version
          echo "🔍 Node.js: $(node --version) | npm: $(npm --version)"
          NODE_MAJOR=$(node --version | cut -d'.' -f1 | tr -d 'v')
          if [ "$NODE_MAJOR" -lt "${{ env.NODE_VERSION }}" ]; then
            echo "❌ Node.js version mismatch! Expected ${{ env.NODE_VERSION }}.x, got $(node --version)"
            exit 1
          fi

          echo "🗑️ Removing old cache versions..."
          find /tmp/runner-cache -maxdepth 1 -type d ! -name "${{ env.CACHE_VERSION }}" ! -name "runner-cache" ! -name "workspace" -exec rm -rf {} + 2>/dev/null || true

          echo "🧹 Clearing stale Playwright and Allure artifacts..."
          rm -rf allure-results test-results backend/allure-results frontend/allure-results 2>/dev/null || true
          mkdir -p allure-results test-results backend/allure-results frontend/allure-results

          echo "📦 Setting up versioned cache directories..."
          mkdir -p /tmp/runner-cache/${{ env.CACHE_VERSION }}/{npm,backend-node_modules,frontend-node_modules,root-node_modules}

          echo "✅ Directories prepared (cache version: ${{ env.CACHE_VERSION }})"

      - name: "📦 Restore/install backend dependencies"
        id: backend-cache
        working-directory: ./backend
        run: |
          CACHE_DIR="/tmp/runner-cache/${{ env.CACHE_VERSION }}/backend-node_modules"
          LOCK_HASH=$(sha256sum package-lock.json | cut -d' ' -f1)

          # Check cache validity
          if [ -f "$CACHE_DIR/.lock-hash" ] && [ -d "$CACHE_DIR/node_modules" ]; then
            CACHED_HASH=$(cat "$CACHE_DIR/.lock-hash")
            if [ "$LOCK_HASH" = "$CACHED_HASH" ]; then
              echo "✅ Cache hit - restoring backend node_modules (hash: ${LOCK_HASH:0:12}...)"
              cp -r "$CACHE_DIR/node_modules" ./
              echo "cache-hit=true" >> $GITHUB_OUTPUT
              exit 0
            fi
            echo "⚠️ Cache invalidated: ${CACHED_HASH:0:12}... → ${LOCK_HASH:0:12}..."
            rm -rf "$CACHE_DIR"
            mkdir -p "$CACHE_DIR"
          else
            echo "❌ No valid cache found"
          fi

          # Install and save to cache
          echo "📦 Installing backend dependencies..."
          npm ci --cache /tmp/runner-cache/${{ env.CACHE_VERSION }}/npm --prefer-offline --include=dev

          echo "💾 Saving to cache..."
          flock -w 60 "$CACHE_DIR/.flock" bash -c "
            rm -rf '$CACHE_DIR/node_modules'
            cp -r node_modules '$CACHE_DIR/'
            echo '$LOCK_HASH' > '$CACHE_DIR/.lock-hash'
          "
          echo "cache-hit=false" >> $GITHUB_OUTPUT
          echo "✅ Backend dependencies installed and cached"

      - name: "📦 Restore/install frontend dependencies"
        id: frontend-cache
        working-directory: ./frontend
        run: |
          CACHE_DIR="/tmp/runner-cache/${{ env.CACHE_VERSION }}/frontend-node_modules"
          LOCK_HASH=$(sha256sum package-lock.json | cut -d' ' -f1)

          # Check cache validity
          if [ -f "$CACHE_DIR/.lock-hash" ] && [ -d "$CACHE_DIR/node_modules" ]; then
            CACHED_HASH=$(cat "$CACHE_DIR/.lock-hash")
            if [ "$LOCK_HASH" = "$CACHED_HASH" ]; then
              echo "✅ Cache hit - restoring frontend node_modules (hash: ${LOCK_HASH:0:12}...)"
              cp -r "$CACHE_DIR/node_modules" ./
              echo "cache-hit=true" >> $GITHUB_OUTPUT
              exit 0
            fi
            echo "⚠️ Cache invalidated: ${CACHED_HASH:0:12}... → ${LOCK_HASH:0:12}..."
            rm -rf "$CACHE_DIR"
            mkdir -p "$CACHE_DIR"
          else
            echo "❌ No valid cache found"
          fi

          # Install and save to cache
          echo "📦 Installing frontend dependencies..."
          npm ci --cache /tmp/runner-cache/${{ env.CACHE_VERSION }}/npm --prefer-offline

          echo "💾 Saving to cache..."
          flock -w 60 "$CACHE_DIR/.flock" bash -c "
            rm -rf '$CACHE_DIR/node_modules'
            cp -r node_modules '$CACHE_DIR/'
            echo '$LOCK_HASH' > '$CACHE_DIR/.lock-hash'
          "
          echo "cache-hit=false" >> $GITHUB_OUTPUT
          echo "✅ Frontend dependencies installed and cached"

      - name: "📦 Restore/install root dependencies"
        run: |
          CACHE_DIR="/tmp/runner-cache/${{ env.CACHE_VERSION }}/root-node_modules"
          LOCK_HASH=$(sha256sum package-lock.json | cut -d' ' -f1)

          # Check cache validity
          if [ -f "$CACHE_DIR/.lock-hash" ] && [ -d "$CACHE_DIR/node_modules" ]; then
            CACHED_HASH=$(cat "$CACHE_DIR/.lock-hash")
            if [ "$LOCK_HASH" = "$CACHED_HASH" ]; then
              echo "✅ Cache hit - restoring root node_modules (hash: ${LOCK_HASH:0:12}...)"
              cp -r "$CACHE_DIR/node_modules" ./
              exit 0
            fi
            echo "⚠️ Cache invalidated: ${CACHED_HASH:0:12}... → ${LOCK_HASH:0:12}..."
            rm -rf "$CACHE_DIR"
            mkdir -p "$CACHE_DIR"
          else
            echo "❌ No valid cache found"
          fi

          # Install and save to cache
          echo "📦 Installing root dependencies (Playwright)..."
          npm ci --cache /tmp/runner-cache/${{ env.CACHE_VERSION }}/npm --prefer-offline

          echo "💾 Saving to cache..."
          flock -w 60 "$CACHE_DIR/.flock" bash -c "
            rm -rf '$CACHE_DIR/node_modules'
            cp -r node_modules '$CACHE_DIR/'
            echo '$LOCK_HASH' > '$CACHE_DIR/.lock-hash'
          "
          echo "✅ Root dependencies installed and cached"

      - name: "🔧 Generate Prisma client & share workspace"
        id: setup-complete
        run: |
          echo "🔧 Generating Prisma client..."
          cd backend && npm run db:generate

          if [ ! -d "src/generated/prisma" ]; then
            echo "❌ Prisma client generation failed"
            exit 1
          fi
          cd ..

          echo "📤 Copying workspace to shared location..."
          SHARED_WORKSPACE="/tmp/runner-cache/workspace/${{ github.run_id }}"
          mkdir -p "$SHARED_WORKSPACE"

          rsync -a --exclude='.git' \
                   --exclude='node_modules' \
                   --exclude='backend/node_modules' \
                   --exclude='frontend/node_modules' \
                   --exclude='*.backup' \
                   . "$SHARED_WORKSPACE/"

          echo "✅ Workspace preparation complete"
          echo "   Backend cache: ${{ steps.backend-cache.outputs.cache-hit && 'hit' || 'miss' }}"
          echo "   Frontend cache: ${{ steps.frontend-cache.outputs.cache-hit && 'hit' || 'miss' }}"
          echo "ready=true" >> $GITHUB_OUTPUT

  # =============================================================================
  # PHASE 1: VALIDATION & TESTING (Security, Unit Tests, Integration Tests)
  # =============================================================================

  validate-and-test:
    name: "🔒🧪 Security, Unit & Integration Tests"
    runs-on: self-hosted
    needs: prepare-workspace
    timeout-minutes: 12
    permissions:
      pull-requests: write
      contents: read
    concurrency:
      group: validate-and-test
      cancel-in-progress: false
    outputs:
      security-passed: ${{ steps.security-results.outputs.passed }}
      unit-tests-passed: ${{ steps.unit-test-results.outputs.passed }}
      integration-tests-passed: ${{ steps.integration-test-results.outputs.passed }}
      coverage-percent: ${{ steps.unit-test-results.outputs.coverage }}

    steps:
      # =========================================================================
      # SETUP (shared across all phases)
      # =========================================================================
      - name: "📥 Copy shared workspace & restore backend/frontend dependencies"
        run: |
          SHARED_WORKSPACE="/tmp/runner-cache/workspace/${{ github.run_id }}"
          if [ ! -d "$SHARED_WORKSPACE" ]; then
            echo "❌ Shared workspace not found at $SHARED_WORKSPACE"
            exit 1
          fi
          rsync -a --delete "$SHARED_WORKSPACE/" .
          echo "✅ Workspace ready (stale files removed)"

          echo "📦 Restoring backend node_modules..."
          cp -r /tmp/runner-cache/${{ env.CACHE_VERSION }}/backend-node_modules/node_modules ./backend/ || exit 1

          echo "📦 Restoring frontend node_modules..."
          cp -r /tmp/runner-cache/${{ env.CACHE_VERSION }}/frontend-node_modules/node_modules ./frontend/ || exit 1

          echo "✅ Dependencies restored"

      - name: "⚡ Setup Node.js runtime environment"
        uses: actions/setup-node@395ad3262231945c25e8478fd5baf05154b1d79f # v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: "📋 Setup warning collection"
        run: |
          mkdir -p .warnings
          # Code quality
          : > .warnings/eslint.log
          : > .warnings/typescript.log
          # Security
          : > .warnings/security.log
          : > .warnings/audit.log
          # Tests
          : > .warnings/tests.log
          : > .warnings/skipped.log
          # Runtime
          : > .warnings/deprecation.log
          : > .warnings/versions.log
          # Infrastructure
          : > .warnings/docker.log
          echo "✅ Warning collection initialized"

      # =========================================================================
      # PHASE 1: SECURITY & CODE QUALITY
      # =========================================================================
      - name: "🔒 Security & code quality: TypeScript, ESLint, npm audit, validations"
        id: security-results
        run: |
          # Verify Node.js version
          echo "🔍 Node.js: $(node --version) | npm: $(npm --version)"
          NODE_MAJOR=$(node --version | cut -d'.' -f1 | tr -d 'v')
          if [ "$NODE_MAJOR" -lt "${{ env.NODE_VERSION }}" ]; then
            echo "❌ Node.js version mismatch!"
            echo "   Expected: ${{ env.NODE_VERSION }}.x"
            echo "   Got: $(node --version)"
            echo "   Fix: Ensure actions/setup-node ran successfully"
            exit 1
          fi

          echo "=========================================="
          echo "🔍 TypeScript type checking"
          echo "=========================================="
          echo "Checking backend..."
          cd backend
          set +e
          npm run typecheck 2>&1 | tee ../tsc-backend.txt
          TSC_BACKEND=$?
          set -e
          # Capture TypeScript warnings/errors for reporting
          grep -E "error TS|warning TS" ../tsc-backend.txt >> ../.warnings/typescript.log || true
          rm -f ../tsc-backend.txt
          if [ $TSC_BACKEND -ne 0 ]; then
            echo "❌ Backend TypeScript check failed"
            exit 1
          fi

          echo "Checking frontend..."
          cd ../frontend
          set +e
          npm run typecheck 2>&1 | tee ../tsc-frontend.txt
          TSC_FRONTEND=$?
          set -e
          grep -E "error TS|warning TS" ../tsc-frontend.txt >> ../.warnings/typescript.log || true
          rm -f ../tsc-frontend.txt
          if [ $TSC_FRONTEND -ne 0 ]; then
            echo "❌ Frontend TypeScript check failed"
            exit 1
          fi
          cd ..
          echo "✅ TypeScript checks passed"

          echo ""
          echo "=========================================="
          echo "🔒 Security linting (ESLint + security rules)"
          echo "=========================================="
          cd backend
          set +e
          npm run lint:security 2>&1 | tee lint-output.txt
          LINT_EXIT=$?
          set -e

          # Extract error count
          ERROR_COUNT=$(grep -oP '✖.*\(\K[0-9]+(?= error)' lint-output.txt || echo "0")

          # Capture warnings for report (deduplicated)
          grep -E "warning.*(typescript-eslint|security/)" lint-output.txt | sort -u > ../.warnings/eslint.log || true
          WARN_SUMMARY=$(grep -oP '✖ \K[0-9]+ problems? \([0-9]+ errors?, [0-9]+ warnings?\)' lint-output.txt || echo "")
          [ -n "$WARN_SUMMARY" ] && echo "Summary: $WARN_SUMMARY" >> ../.warnings/eslint.log

          rm -f lint-output.txt
          if [ "$ERROR_COUNT" != "0" ]; then
            echo "❌ ESLint found $ERROR_COUNT errors"
            exit 1
          fi
          echo "✅ Security linting passed (warnings captured for report)"
          cd ..

          echo ""
          echo "=========================================="
          echo "🛡️ npm security audit"
          echo "=========================================="
          cd backend
          set +e
          npm audit --audit-level=moderate 2>&1 | tee ../audit-output.txt
          AUDIT_EXIT=$?
          set -e

          # Capture vulnerability summary for report
          grep -E "vulnerabilities|moderate|high|critical" ../audit-output.txt >> ../.warnings/audit.log || true
          rm -f ../audit-output.txt

          if [ $AUDIT_EXIT -ne 0 ]; then
            echo "❌ npm audit found security vulnerabilities"
            exit 1
          fi
          echo "✅ npm audit passed"

          echo ""
          echo "=========================================="
          echo "🔒 Custom security validation"
          echo "=========================================="
          node scripts/validate-security.js 2>&1 | tee ../security-val.txt
          grep -iE "WARN|warning" ../security-val.txt >> ../.warnings/security.log || true
          rm -f ../security-val.txt
          cd ..

          echo ""
          echo "=========================================="
          echo "🛡️ Test integrity validation"
          echo "=========================================="
          if [ -f "./scripts/validate-test-integrity.sh" ]; then
            ./scripts/validate-test-integrity.sh
          else
            echo "⚠️ Test integrity script not found - skipping"
          fi

          echo ""
          echo "=========================================="
          echo "🔐 Environment & secrets validation"
          echo "=========================================="
          if [ -f "./scripts/validate-environment.sh" ]; then
            ./scripts/validate-environment.sh build || exit 1
          fi
          echo "✅ Environment validation passed"

          echo ""
          echo "=========================================="
          echo "🔐 Enhanced security audit (20-user prototype)"
          echo "=========================================="
          ./scripts/security-audit.sh || exit 1

          echo ""
          echo "✅ All security & code quality checks passed"
          echo "passed=true" >> $GITHUB_OUTPUT

      # =========================================================================
      # PHASE 2: START SHARED TEST CONTAINERS
      # =========================================================================
      - name: "🐳 Start shared postgres/redis containers & create unit + integration databases"
        run: |
          cat > docker-compose.test.yml << 'EOF'
          version: '3.8'
          services:
            postgres:
              image: postgres:15-alpine
              container_name: loyalty_postgres_test
              environment:
                POSTGRES_USER: loyalty
                POSTGRES_PASSWORD: loyalty_password
                POSTGRES_DB: loyalty_db
              ports:
                - "5438:5432"
              volumes:
                - loyalty_postgres_test_data:/var/lib/postgresql/data
              healthcheck:
                test: ["CMD-SHELL", "pg_isready -U loyalty -d loyalty_db"]
                interval: 5s
                timeout: 5s
                retries: 5

            redis:
              image: redis:7-alpine
              container_name: loyalty_redis_test
              ports:
                - "6383:6379"
              healthcheck:
                test: ["CMD", "redis-cli", "ping"]
                interval: 5s
                timeout: 3s
                retries: 5

          volumes:
            loyalty_postgres_test_data:
          EOF

          # Cleanup and start
          docker compose -f docker-compose.test.yml down -v 2>/dev/null || true
          docker compose -f docker-compose.test.yml up -d 2>&1 | tee docker-output.txt
          grep -iE "level=warning|WARN|orphan" docker-output.txt >> .warnings/docker.log || true
          rm -f docker-output.txt

          # Wait for services
          echo "⏳ Waiting for postgres..."
          timeout 60 bash -c 'until docker exec loyalty_postgres_test pg_isready -U loyalty; do sleep 2; done'
          echo "⏳ Waiting for redis..."
          timeout 30 bash -c 'until docker exec loyalty_redis_test redis-cli ping | grep -q PONG; do sleep 2; done'

          # Create both test databases
          echo "🗄️ Creating test databases..."
          docker exec loyalty_postgres_test psql -U loyalty -d postgres -c "CREATE USER loyalty_unit WITH PASSWORD 'unit_password';" || true
          docker exec loyalty_postgres_test psql -U loyalty -d postgres -c "CREATE DATABASE loyalty_unit_db OWNER loyalty_unit;" || true
          docker exec loyalty_postgres_test psql -U loyalty -d postgres -c "GRANT ALL PRIVILEGES ON DATABASE loyalty_unit_db TO loyalty_unit;" || true
          docker exec loyalty_postgres_test psql -U loyalty_unit -d loyalty_unit_db -c "CREATE EXTENSION IF NOT EXISTS \"uuid-ossp\";" || true

          docker exec loyalty_postgres_test psql -U loyalty -d postgres -c "CREATE USER loyalty_integration WITH PASSWORD 'integration_password';" || true
          docker exec loyalty_postgres_test psql -U loyalty -d postgres -c "CREATE DATABASE loyalty_integration_db OWNER loyalty_integration;" || true
          docker exec loyalty_postgres_test psql -U loyalty -d postgres -c "GRANT ALL PRIVILEGES ON DATABASE loyalty_integration_db TO loyalty_integration;" || true
          docker exec loyalty_postgres_test psql -U loyalty_integration -d loyalty_integration_db -c "CREATE EXTENSION IF NOT EXISTS \"uuid-ossp\";" || true

          echo "✅ Test containers ready (postgres:5438, redis:6383)"
          echo "   - loyalty_unit_db for unit tests"
          echo "   - loyalty_integration_db for integration tests"

      # =========================================================================
      # PHASE 3: UNIT TESTS
      # =========================================================================
      - name: "🧪 Unit tests: setup database, run Prisma migrations, execute tests with coverage"
        id: run-unit-tests
        working-directory: ./backend
        env:
          DATABASE_URL: postgresql://loyalty_unit:unit_password@localhost:5438/loyalty_unit_db
          REDIS_URL: redis://localhost:6383
          NODE_ENV: test
          JWT_SECRET: test-jwt-secret-for-testing-only-must-be-64-chars-minimum-xxxxxx
          JWT_REFRESH_SECRET: test-refresh-secret-for-testing-only-must-be-64-chars-minimum-xx
          SESSION_SECRET: test-session-secret-for-testing-only-min-32-chars
        run: |
          echo "🔧 Setting up unit test database..."
          npm run db:generate
          npm run db:migrate:deploy
          echo "✅ Unit test database ready"

          echo ""
          echo "🧪 Running unit tests with coverage..."
          npm run test:unit -- --coverage --passWithNoTests 2>&1 | tee ../test-unit-output.txt

          # Capture deprecation warnings
          grep -E "\[DEP[0-9]+\]|DeprecationWarning" ../test-unit-output.txt | sort -u >> ../.warnings/deprecation.log || true

          # Capture skipped/pending tests
          grep -E "○ skipped|⊘ pending|› .* \(skipped\)|test\.skip|it\.skip" ../test-unit-output.txt >> ../.warnings/skipped.log || true

          # Capture test warnings and console.warn output
          grep -iE "console\.warn|WARN:" ../test-unit-output.txt >> ../.warnings/tests.log || true

          # Capture test summary for failures
          grep -E "Tests:.*failed|failed,.*passed" ../test-unit-output.txt >> ../.warnings/tests.log || true

          rm -f ../test-unit-output.txt

      - name: "📊 Upload unit test coverage report (retained 30 days)"
        if: always()
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5
        with:
          name: coverage-report-${{ github.run_id }}
          path: backend/coverage/
          retention-days: 30
          if-no-files-found: warn

      - name: "📊 Upload unit test Allure results (retained 7 days)"
        if: always()
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5
        with:
          name: allure-results-unit-${{ github.run_id }}
          path: backend/allure-results/
          retention-days: 7
          if-no-files-found: warn

      - name: "📈 Evaluate unit test results & extract coverage metrics"
        id: unit-test-results
        if: always()
        run: |
          if [ "${{ steps.run-unit-tests.outcome }}" == "success" ]; then
            echo "✅ Unit tests passed"
            echo "passed=true" >> $GITHUB_OUTPUT
          else
            echo "❌ Unit tests failed"
            echo "passed=false" >> $GITHUB_OUTPUT
          fi

          if [ -f backend/coverage/coverage-summary.json ]; then
            COVERAGE=$(jq '.total.lines.pct' backend/coverage/coverage-summary.json)
            echo "coverage=$COVERAGE" >> $GITHUB_OUTPUT
            echo "📊 Coverage: $COVERAGE%"
          else
            echo "coverage=0" >> $GITHUB_OUTPUT
          fi

      # =========================================================================
      # PHASE 4: INTEGRATION TESTS
      # =========================================================================
      - name: "🔗 Integration tests: setup database, run Prisma migrations, execute API tests"
        id: run-integration-tests
        working-directory: ./backend
        env:
          DATABASE_URL: postgresql://loyalty_integration:integration_password@localhost:5438/loyalty_integration_db
          REDIS_URL: redis://localhost:6383
          NODE_ENV: test
          JWT_SECRET: test-jwt-secret-for-testing-only-must-be-64-chars-minimum-xxxxxx
          JWT_REFRESH_SECRET: test-refresh-secret-for-testing-only-must-be-64-chars-minimum-xx
          SESSION_SECRET: test-session-secret-for-testing-only-min-32-chars
        run: |
          echo "🔧 Setting up integration test database..."
          npm run db:migrate:deploy
          echo "✅ Integration test database ready"

          echo ""
          echo "🔗 Running integration tests..."
          npm run test:integration 2>&1 | tee ../test-integration-output.txt

          # Capture deprecation warnings
          grep -E "\[DEP[0-9]+\]|DeprecationWarning" ../test-integration-output.txt | sort -u >> ../.warnings/deprecation.log || true

          # Capture skipped/pending tests
          grep -E "○ skipped|⊘ pending|› .* \(skipped\)|test\.skip|it\.skip" ../test-integration-output.txt >> ../.warnings/skipped.log || true

          # Capture test warnings and console.warn output
          grep -iE "console\.warn|WARN:" ../test-integration-output.txt >> ../.warnings/tests.log || true

          # Capture test summary for failures
          grep -E "Tests:.*failed|failed,.*passed" ../test-integration-output.txt >> ../.warnings/tests.log || true

          rm -f ../test-integration-output.txt

      - name: "📊 Upload integration test Allure results (retained 7 days)"
        if: always()
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5
        with:
          name: allure-results-integration-${{ github.run_id }}
          path: backend/allure-results/
          retention-days: 7
          if-no-files-found: warn

      - name: "🗄️ Database schema tests & migration validation"
        id: run-db-tests
        working-directory: ./backend
        env:
          DATABASE_URL: postgresql://loyalty_integration:integration_password@localhost:5438/loyalty_integration_db
          NODE_ENV: test
        run: |
          echo "🧪 Running database schema tests..."
          npm run test:db -- --passWithNoTests

          echo ""
          echo "🗄️ Running database migration validation..."
          cd ..
          if [ -f "./scripts/validate-db-migration.sh" ]; then
            ./scripts/validate-db-migration.sh || echo "⚠️ DB migration validation failed (non-blocking)"
          fi
          echo "✅ Database validation complete"

      - name: "📈 Evaluate integration test results (API tests + DB schema tests)"
        id: integration-test-results
        if: always()
        run: |
          INTEGRATION="${{ steps.run-integration-tests.outcome }}"
          DB_TESTS="${{ steps.run-db-tests.outcome }}"

          if [ "$INTEGRATION" == "success" ] && [ "$DB_TESTS" == "success" ]; then
            echo "✅ All integration tests passed"
            echo "passed=true" >> $GITHUB_OUTPUT
          else
            echo "❌ Integration tests failed (integration: $INTEGRATION, db: $DB_TESTS)"
            echo "passed=false" >> $GITHUB_OUTPUT
            exit 1
          fi

      - name: "📊 Write test summary to job summary"
        if: always()
        run: |
          UNIT_PASSED="${{ steps.unit-test-results.outputs.passed }}"
          INTEGRATION_PASSED="${{ steps.integration-test-results.outputs.passed }}"

          # Determine status emoji
          if [ "$UNIT_PASSED" == "true" ]; then
            UNIT_STATUS="✅ Passed"
          else
            UNIT_STATUS="❌ Failed"
          fi

          if [ "$INTEGRATION_PASSED" == "true" ]; then
            INTEGRATION_STATUS="✅ Passed"
          else
            INTEGRATION_STATUS="❌ Failed"
          fi

          # Start summary
          echo "## 🧪 Unit & Integration Test Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Test Suite | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|:-----------|:-------|" >> $GITHUB_STEP_SUMMARY
          echo "| Unit Tests | $UNIT_STATUS |" >> $GITHUB_STEP_SUMMARY
          echo "| Integration Tests | $INTEGRATION_STATUS |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Coverage report
          if [ -f backend/coverage/coverage-summary.json ]; then
            echo "## 📊 Test Coverage Report" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY

            LINES_COVERED=$(jq '.total.lines.covered' backend/coverage/coverage-summary.json)
            LINES_TOTAL=$(jq '.total.lines.total' backend/coverage/coverage-summary.json)
            LINES_PCT=$(jq '.total.lines.pct' backend/coverage/coverage-summary.json)

            STMTS_COVERED=$(jq '.total.statements.covered' backend/coverage/coverage-summary.json)
            STMTS_TOTAL=$(jq '.total.statements.total' backend/coverage/coverage-summary.json)
            STMTS_PCT=$(jq '.total.statements.pct' backend/coverage/coverage-summary.json)

            FUNCS_COVERED=$(jq '.total.functions.covered' backend/coverage/coverage-summary.json)
            FUNCS_TOTAL=$(jq '.total.functions.total' backend/coverage/coverage-summary.json)
            FUNCS_PCT=$(jq '.total.functions.pct' backend/coverage/coverage-summary.json)

            BRANCHES_COVERED=$(jq '.total.branches.covered' backend/coverage/coverage-summary.json)
            BRANCHES_TOTAL=$(jq '.total.branches.total' backend/coverage/coverage-summary.json)
            BRANCHES_PCT=$(jq '.total.branches.pct' backend/coverage/coverage-summary.json)

            echo "| Metric | Covered | Total | Percentage |" >> $GITHUB_STEP_SUMMARY
            echo "|:-------|--------:|------:|-----------:|" >> $GITHUB_STEP_SUMMARY
            echo "| Lines | $LINES_COVERED | $LINES_TOTAL | ${LINES_PCT}% |" >> $GITHUB_STEP_SUMMARY
            echo "| Statements | $STMTS_COVERED | $STMTS_TOTAL | ${STMTS_PCT}% |" >> $GITHUB_STEP_SUMMARY
            echo "| Functions | $FUNCS_COVERED | $FUNCS_TOTAL | ${FUNCS_PCT}% |" >> $GITHUB_STEP_SUMMARY
            echo "| Branches | $BRANCHES_COVERED | $BRANCHES_TOTAL | ${BRANCHES_PCT}% |" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
          fi

          # Download reports info
          echo "## 📥 Download Reports" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Coverage reports are available as workflow artifacts for 30 days." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Report | Path |" >> $GITHUB_STEP_SUMMARY
          echo "|:-------|:-----|" >> $GITHUB_STEP_SUMMARY
          echo "| HTML Report | \`backend/coverage/lcov-report/index.html\` |" >> $GITHUB_STEP_SUMMARY
          echo "| LCOV Data | \`backend/coverage/lcov.info\` |" >> $GITHUB_STEP_SUMMARY
          echo "| JSON Summary | \`backend/coverage/coverage-summary.json\` |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

      # =========================================================================
      # PHASE 5: WARNINGS COLLECTION & REPORT
      # =========================================================================
      - name: "📦 Check for outdated packages"
        if: always()
        continue-on-error: true
        run: |
          echo "=== Backend ===" >> .warnings/versions.log
          cd backend
          npm outdated 2>&1 | head -20 >> ../.warnings/versions.log || true

          echo "" >> ../.warnings/versions.log
          echo "=== Frontend ===" >> ../.warnings/versions.log
          cd ../frontend
          npm outdated 2>&1 | head -20 >> ../.warnings/versions.log || true

      - name: "📊 Generate consolidated warnings report"
        if: always()
        run: |
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "## ⚠️ Pipeline Warnings Report" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Count warnings per category (use wc -l to avoid grep exit code issues)
          ESLINT_COUNT=$(wc -l < .warnings/eslint.log 2>/dev/null | tr -d ' ') || ESLINT_COUNT=0
          TS_COUNT=$(wc -l < .warnings/typescript.log 2>/dev/null | tr -d ' ') || TS_COUNT=0
          SECURITY_COUNT=$(wc -l < .warnings/security.log 2>/dev/null | tr -d ' ') || SECURITY_COUNT=0
          AUDIT_COUNT=$(wc -l < .warnings/audit.log 2>/dev/null | tr -d ' ') || AUDIT_COUNT=0
          TESTS_COUNT=$(wc -l < .warnings/tests.log 2>/dev/null | tr -d ' ') || TESTS_COUNT=0
          SKIPPED_COUNT=$(wc -l < .warnings/skipped.log 2>/dev/null | tr -d ' ') || SKIPPED_COUNT=0
          DEPRECATION_COUNT=$(wc -l < .warnings/deprecation.log 2>/dev/null | tr -d ' ') || DEPRECATION_COUNT=0
          VERSIONS_COUNT=$(wc -l < .warnings/versions.log 2>/dev/null | tr -d ' ') || VERSIONS_COUNT=0
          DOCKER_COUNT=$(wc -l < .warnings/docker.log 2>/dev/null | tr -d ' ') || DOCKER_COUNT=0

          TOTAL=$((ESLINT_COUNT + TS_COUNT + SECURITY_COUNT + AUDIT_COUNT + TESTS_COUNT + SKIPPED_COUNT + DEPRECATION_COUNT + VERSIONS_COUNT + DOCKER_COUNT))

          if [ "$TOTAL" -gt 0 ]; then
            echo "> **Note:** Warnings are informational only and do not affect build status." >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY

            # Summary table
            echo "### 📊 Summary" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "| Category | Count |" >> $GITHUB_STEP_SUMMARY
            echo "|:---------|------:|" >> $GITHUB_STEP_SUMMARY

            # Code Quality
            [ "$ESLINT_COUNT" -gt 0 ] && echo "| 🔍 ESLint | $ESLINT_COUNT |" >> $GITHUB_STEP_SUMMARY
            [ "$TS_COUNT" -gt 0 ] && echo "| 📝 TypeScript | $TS_COUNT |" >> $GITHUB_STEP_SUMMARY

            # Security
            [ "$SECURITY_COUNT" -gt 0 ] && echo "| 🔒 Security | $SECURITY_COUNT |" >> $GITHUB_STEP_SUMMARY
            [ "$AUDIT_COUNT" -gt 0 ] && echo "| 🛡️ npm Audit | $AUDIT_COUNT |" >> $GITHUB_STEP_SUMMARY

            # Tests
            [ "$TESTS_COUNT" -gt 0 ] && echo "| 🧪 Test Warnings | $TESTS_COUNT |" >> $GITHUB_STEP_SUMMARY
            [ "$SKIPPED_COUNT" -gt 0 ] && echo "| ⏭️ Skipped Tests | $SKIPPED_COUNT |" >> $GITHUB_STEP_SUMMARY

            # Runtime
            [ "$DEPRECATION_COUNT" -gt 0 ] && echo "| ⚠️ Deprecations | $DEPRECATION_COUNT |" >> $GITHUB_STEP_SUMMARY
            [ "$VERSIONS_COUNT" -gt 0 ] && echo "| 📦 Outdated Packages | $VERSIONS_COUNT |" >> $GITHUB_STEP_SUMMARY

            # Infrastructure
            [ "$DOCKER_COUNT" -gt 0 ] && echo "| 🐳 Docker | $DOCKER_COUNT |" >> $GITHUB_STEP_SUMMARY

            echo "| **Total** | **$TOTAL** |" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY

            echo "<details>" >> $GITHUB_STEP_SUMMARY
            echo "<summary>📋 Click to expand warning details</summary>" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY

            # ESLint warnings
            if [ -s ".warnings/eslint.log" ]; then
              echo "### 🔍 ESLint" >> $GITHUB_STEP_SUMMARY
              echo '```' >> $GITHUB_STEP_SUMMARY
              head -50 ".warnings/eslint.log" >> $GITHUB_STEP_SUMMARY
              [ $(wc -l < ".warnings/eslint.log") -gt 50 ] && echo "... (truncated, showing first 50 lines)" >> $GITHUB_STEP_SUMMARY
              echo '```' >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
            fi

            # TypeScript warnings
            if [ -s ".warnings/typescript.log" ]; then
              echo "### 📝 TypeScript" >> $GITHUB_STEP_SUMMARY
              echo '```' >> $GITHUB_STEP_SUMMARY
              head -50 ".warnings/typescript.log" >> $GITHUB_STEP_SUMMARY
              [ $(wc -l < ".warnings/typescript.log") -gt 50 ] && echo "... (truncated, showing first 50 lines)" >> $GITHUB_STEP_SUMMARY
              echo '```' >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
            fi

            # Security warnings
            if [ -s ".warnings/security.log" ]; then
              echo "### 🔒 Security" >> $GITHUB_STEP_SUMMARY
              echo '```' >> $GITHUB_STEP_SUMMARY
              head -50 ".warnings/security.log" >> $GITHUB_STEP_SUMMARY
              [ $(wc -l < ".warnings/security.log") -gt 50 ] && echo "... (truncated, showing first 50 lines)" >> $GITHUB_STEP_SUMMARY
              echo '```' >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
            fi

            # npm Audit warnings
            if [ -s ".warnings/audit.log" ]; then
              echo "### 🛡️ npm Audit" >> $GITHUB_STEP_SUMMARY
              echo '```' >> $GITHUB_STEP_SUMMARY
              head -50 ".warnings/audit.log" >> $GITHUB_STEP_SUMMARY
              [ $(wc -l < ".warnings/audit.log") -gt 50 ] && echo "... (truncated, showing first 50 lines)" >> $GITHUB_STEP_SUMMARY
              echo '```' >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
            fi

            # Test warnings
            if [ -s ".warnings/tests.log" ]; then
              echo "### 🧪 Test Warnings" >> $GITHUB_STEP_SUMMARY
              echo '```' >> $GITHUB_STEP_SUMMARY
              head -50 ".warnings/tests.log" >> $GITHUB_STEP_SUMMARY
              [ $(wc -l < ".warnings/tests.log") -gt 50 ] && echo "... (truncated, showing first 50 lines)" >> $GITHUB_STEP_SUMMARY
              echo '```' >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
            fi

            # Skipped tests
            if [ -s ".warnings/skipped.log" ]; then
              echo "### ⏭️ Skipped Tests" >> $GITHUB_STEP_SUMMARY
              echo '```' >> $GITHUB_STEP_SUMMARY
              head -50 ".warnings/skipped.log" >> $GITHUB_STEP_SUMMARY
              [ $(wc -l < ".warnings/skipped.log") -gt 50 ] && echo "... (truncated, showing first 50 lines)" >> $GITHUB_STEP_SUMMARY
              echo '```' >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
            fi

            # Deprecation warnings
            if [ -s ".warnings/deprecation.log" ]; then
              echo "### ⚠️ Deprecations" >> $GITHUB_STEP_SUMMARY
              echo '```' >> $GITHUB_STEP_SUMMARY
              head -50 ".warnings/deprecation.log" >> $GITHUB_STEP_SUMMARY
              [ $(wc -l < ".warnings/deprecation.log") -gt 50 ] && echo "... (truncated, showing first 50 lines)" >> $GITHUB_STEP_SUMMARY
              echo '```' >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
            fi

            # Version warnings
            if [ -s ".warnings/versions.log" ]; then
              echo "### 📦 Outdated Packages" >> $GITHUB_STEP_SUMMARY
              echo '```' >> $GITHUB_STEP_SUMMARY
              head -50 ".warnings/versions.log" >> $GITHUB_STEP_SUMMARY
              [ $(wc -l < ".warnings/versions.log") -gt 50 ] && echo "... (truncated, showing first 50 lines)" >> $GITHUB_STEP_SUMMARY
              echo '```' >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
            fi

            # Docker warnings
            if [ -s ".warnings/docker.log" ]; then
              echo "### 🐳 Docker" >> $GITHUB_STEP_SUMMARY
              echo '```' >> $GITHUB_STEP_SUMMARY
              head -50 ".warnings/docker.log" >> $GITHUB_STEP_SUMMARY
              [ $(wc -l < ".warnings/docker.log") -gt 50 ] && echo "... (truncated, showing first 50 lines)" >> $GITHUB_STEP_SUMMARY
              echo '```' >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
            fi

            echo "</details>" >> $GITHUB_STEP_SUMMARY
          else
            echo "✅ No warnings detected in this pipeline run." >> $GITHUB_STEP_SUMMARY
          fi

          # Save warning count for PR comment
          echo "TOTAL_WARNINGS=$TOTAL" >> $GITHUB_ENV
          echo "SKIPPED_TESTS=$SKIPPED_COUNT" >> $GITHUB_ENV

      # =========================================================================
      # FINAL: PR COMMENT & CLEANUP
      # =========================================================================
      - name: "💬 Post test results summary to pull request (unit + integration status)"
        if: always() && github.event_name == 'pull_request'
        uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd # v8
        continue-on-error: true
        with:
          script: |
            const fs = require('fs');
            const unitPassed = '${{ steps.unit-test-results.outputs.passed }}' === 'true';
            const integrationPassed = '${{ steps.integration-test-results.outputs.passed }}' === 'true';

            const unitEmoji = unitPassed ? '✅' : '❌';
            const integrationEmoji = integrationPassed ? '✅' : '❌';

            let coverageSection = '';
            const coveragePath = 'backend/coverage/coverage-summary.json';
            if (fs.existsSync(coveragePath)) {
              const data = JSON.parse(fs.readFileSync(coveragePath, 'utf8'));
              const t = data.total;
              coverageSection = [
                '', '### 📊 Coverage',
                '| Metric | Covered | Total | % |',
                '|--------|---------|-------|---|',
                `| Lines | ${t.lines.covered} | ${t.lines.total} | ${t.lines.pct}% |`,
                `| Functions | ${t.functions.covered} | ${t.functions.total} | ${t.functions.pct}% |`,
                `| Branches | ${t.branches.covered} | ${t.branches.total} | ${t.branches.pct}% |`,
              ].join('\n');
            }

            // Add warnings section
            const totalWarnings = '${{ env.TOTAL_WARNINGS }}' || '0';
            const skippedTests = '${{ env.SKIPPED_TESTS }}' || '0';
            let warningsSection = '';
            if (parseInt(totalWarnings) > 0) {
              warningsSection = [
                '', '### ⚠️ Warnings',
                '| Category | Count |',
                '|----------|------:|',
                `| Total Warnings | ${totalWarnings} |`,
                `| Skipped Tests | ${skippedTests} |`,
                '',
                `> See [Job Summary](https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}) for details.`,
              ].join('\n');
            }

            const allPassed = unitPassed && integrationPassed;
            const comment = [
              '## 🧪 CI Test Results',
              '',
              '| Stage | Status |',
              '|-------|--------|',
              `| Unit Tests | ${unitEmoji} ${unitPassed ? 'Passed' : 'Failed'} |`,
              `| Integration Tests | ${integrationEmoji} ${integrationPassed ? 'Passed' : 'Failed'} |`,
              '| E2E Tests | ⏳ Pending |',
              coverageSection,
              warningsSection,
              '',
              allPassed ? '> E2E tests starting next...' : '> ❌ Tests failed - pipeline stopped.',
              '',
              `> Commit: \`${context.sha.substring(0, 7)}\` | [Artifacts](https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId})`,
            ].join('\n');

            const MARKER = '<!-- CI-TEST-RESULTS-COMMENT -->';
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner, repo: context.repo.repo, issue_number: context.issue.number,
            });
            const existing = comments.find(c => c.user.type === 'Bot' && c.body.includes(MARKER));

            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner, repo: context.repo.repo,
                comment_id: existing.id, body: MARKER + '\n' + comment
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner, repo: context.repo.repo,
                issue_number: context.issue.number, body: MARKER + '\n' + comment
              });
            }

      - name: "🧹 Cleanup: stop postgres/redis containers, remove volumes and compose file"
        if: always()
        run: |
          echo "🧹 Cleaning up test containers..."
          docker stop loyalty_postgres_test loyalty_redis_test 2>/dev/null || true
          docker rm -f loyalty_postgres_test loyalty_redis_test 2>/dev/null || true
          docker volume rm loyalty_postgres_test_data 2>/dev/null || true
          rm -f docker-compose.test.yml || true
          rm -rf .warnings/ || true
          echo "✅ Cleanup complete"

  # Job 1D: E2E Tests (Conditional - only on main branch or PR to main)
  e2e-tests:
    name: "🎭 E2E Tests"
    runs-on: self-hosted
    permissions:
      pull-requests: write  # For PR comments on test results
      contents: read
    env:
      COMPOSE_PROJECT_NAME: loyalty-e2e-tests

    # Prevent parallel E2E runs to avoid port conflicts with host network
    concurrency:
      group: e2e-tests
      cancel-in-progress: false

    needs: [validate-and-test]  # Sequential: runs after validation and tests pass
    timeout-minutes: 20  # Increased from 8: allows ~15min for 36 tests + 3-5min infrastructure setup
    if: (github.ref == 'refs/heads/main' || github.base_ref == 'main')
    outputs:
      e2e-passed: ${{ steps.e2e-results.outputs.passed }}

    steps:
      - name: "📥 Copy shared workspace & restore root/backend/frontend dependencies"
        run: |
          SHARED_WORKSPACE="/tmp/runner-cache/workspace/${{ github.run_id }}"
          if [ ! -d "$SHARED_WORKSPACE" ]; then
            echo "❌ Shared workspace not found at $SHARED_WORKSPACE"
            exit 1
          fi
          rsync -a --delete "$SHARED_WORKSPACE/" .
          echo "✅ Workspace ready (stale files removed)"

          echo "📦 Restoring root node_modules (Playwright)..."
          cp -r /tmp/runner-cache/${{ env.CACHE_VERSION }}/root-node_modules/node_modules ./ || exit 1

          echo "📦 Restoring backend node_modules..."
          cp -r /tmp/runner-cache/${{ env.CACHE_VERSION }}/backend-node_modules/node_modules ./backend/ || exit 1

          echo "📦 Restoring frontend node_modules..."
          cp -r /tmp/runner-cache/${{ env.CACHE_VERSION }}/frontend-node_modules/node_modules ./frontend/ || exit 1

          echo "✅ All dependencies restored"

      - name: "⚡ Setup Node.js runtime environment"
        uses: actions/setup-node@395ad3262231945c25e8478fd5baf05154b1d79f # v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: "🧹 Cleanup E2E ports, validate Prisma client & remove stale volumes"
        run: |
          # Verify Node.js version
          echo "🔍 Node.js: $(node --version) | npm: $(npm --version)"
          NODE_MAJOR=$(node --version | cut -d'.' -f1 | tr -d 'v')
          if [ "$NODE_MAJOR" -lt "${{ env.NODE_VERSION }}" ]; then
            echo "❌ Node.js version mismatch! Expected ${{ env.NODE_VERSION }}.x, got $(node --version)"
            exit 1
          fi

          echo "🧹 Cleaning up E2E ports (5436, 6381, 3201, 4202)..."
          for port in 5436 6381 3201 4202; do
            pid=$(lsof -ti:$port 2>/dev/null || true)
            if [ ! -z "$pid" ]; then
              echo "⚠️ Killing process on port $port (PID: $pid)"
              kill -9 $pid 2>/dev/null || true
            fi
          done
          sleep 2
          echo "✅ Port cleanup completed"

          echo ""
          echo "🔍 Validating Prisma client from shared workspace..."
          if [ ! -d "backend/src/generated/prisma" ]; then
            echo "❌ Prisma client not found - workspace preparation may have failed"
            exit 1
          fi
          echo "✅ Prisma client validated"

          echo ""
          echo "🧹 Removing stale E2E database volumes..."
          docker volume rm loyalty_postgres_e2e_data loyalty_redis_e2e_data 2>/dev/null || true
          docker volume rm loyalty-e2e-tests_loyalty_postgres_e2e_data loyalty-e2e-tests_loyalty_redis_e2e_data 2>/dev/null || true
          echo "✅ E2E environment prepared"

      - name: "🏗️ Build backend & frontend applications sequentially"
        run: |
          echo "🏗️ Building backend..."
          cd backend && npm run build && cd ..

          echo "🏗️ Building frontend..."
          cd frontend && npm run build && cd ..

          echo "✅ Both applications built successfully"

      - name: "🐳 Create Docker Compose config, start E2E stack & wait for services"
        working-directory: .
        env:
          DATABASE_URL: postgresql://loyalty:loyalty_password@localhost:5436/loyalty_db
          NODE_ENV: development
          # OAuth Configuration for E2E tests (from GitHub Secrets)
          GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID || 'test-google-not-configured' }}
          GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET || 'test-google-secret-not-configured' }}
          LINE_CHANNEL_ID: ${{ secrets.LINE_CHANNEL_ID || 'test-line-not-configured' }}
          LINE_CHANNEL_SECRET: ${{ secrets.LINE_CHANNEL_SECRET || 'test-line-secret-not-configured' }}
        run: |
          echo "🚀 Starting isolated application stack for E2E tests..."

          # Create E2E admin config (overwrites default for E2E testing)
          # This gets baked into the Docker image during build
          echo "📝 Creating E2E admin configuration..."
          mkdir -p backend/config
          cat > backend/config/admins.json << 'ADMINS_EOF'
          {
            "adminEmails": ["e2e-admin@test.local"],
            "superAdminEmails": ["e2e-admin@test.local"],
            "description": "E2E test admin configuration - created by CI workflow"
          }
          ADMINS_EOF
          echo "✅ E2E admin config created (will be included in Docker build)"

          # Create the E2E Docker Compose file with host networking
          echo "📝 Creating E2E Docker Compose configuration with host network..."
          cat > docker-compose.e2e.ci.yml << EOF
          services:
            postgres:
              image: postgres:15-alpine
              container_name: loyalty_postgres_e2e
              network_mode: host
              environment:
                POSTGRES_USER: loyalty
                POSTGRES_PASSWORD: loyalty_password
                POSTGRES_DB: loyalty_db
                PGPORT: 5436
              tmpfs:
                - /dev/shm:size=256m
              volumes:
                - loyalty_postgres_e2e_data:/var/lib/postgresql/data
              healthcheck:
                test: ["CMD-SHELL", "pg_isready -U loyalty -d loyalty_db -p 5436"]
                interval: 5s
                timeout: 5s
                retries: 5
                start_period: 10s

            redis:
              image: redis:7-alpine
              container_name: loyalty_redis_e2e
              network_mode: host
              command: redis-server --port 6381
              volumes:
                - loyalty_redis_e2e_data:/data
              healthcheck:
                test: ["CMD", "redis-cli", "-p", "6381", "ping"]
                interval: 5s
                timeout: 3s
                retries: 5

            backend:
              build:
                context: ./backend
                dockerfile: Dockerfile
                target: development
              container_name: loyalty_backend_e2e
              network_mode: host
              environment:
                NODE_ENV: development
                PORT: 4202
                DATABASE_URL: postgresql://loyalty:loyalty_password@localhost:5436/loyalty_db
                REDIS_URL: redis://localhost:6381
                JWT_SECRET: e2e-jwt-secret-for-testing-only-must-be-64-chars-minimum-xxxxxxx
                JWT_REFRESH_SECRET: e2e-refresh-secret-for-testing-only-must-be-64-chars-minimum-xxx
                SESSION_SECRET: e2e-session-secret-for-testing-only-min-32-chars
                FRONTEND_URL: http://localhost:3201
                BACKEND_URL: http://localhost:4202
                CORS_ORIGINS: "http://localhost:3201"
                # OAuth Configuration (from GitHub Secrets)
                GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
                GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET}
                GOOGLE_CALLBACK_URL: http://localhost:4202/api/oauth/google/callback
                LINE_CHANNEL_ID: ${LINE_CHANNEL_ID}
                LINE_CHANNEL_SECRET: ${LINE_CHANNEL_SECRET}
                LINE_CALLBACK_URL: http://localhost:4202/api/oauth/line/callback
              depends_on:
                postgres:
                  condition: service_healthy
                redis:
                  condition: service_healthy
              healthcheck:
                test: ["CMD-SHELL", "curl -f http://localhost:4202/api/health || exit 1"]
                interval: 10s
                timeout: 5s
                retries: 5
                start_period: 30s

            frontend:
              build:
                context: ./frontend
                dockerfile: Dockerfile
                target: development
              container_name: loyalty_frontend_e2e
              network_mode: host
              environment:
                NODE_ENV: development
                PORT: 3201
                VITE_API_URL: http://localhost:4202/api
              depends_on:
                backend:
                  condition: service_healthy
              healthcheck:
                test: ["CMD-SHELL", "curl -f http://localhost:3201 || exit 1"]
                interval: 10s
                timeout: 5s
                retries: 3

          volumes:
            loyalty_postgres_e2e_data:
              driver: local
            loyalty_redis_e2e_data:
              driver: local
          EOF
          
          echo "✅ E2E Docker Compose configuration created"
          
          # Use project-specific compose teardown to avoid stopping production containers
          echo "🧹 Cleaning up any existing E2E containers and ports..."
          docker compose -p loyalty-e2e-tests -f docker-compose.e2e.ci.yml down -v --remove-orphans 2>/dev/null || true
          
          # Check for processes using E2E ports and kill them
          echo "🔍 Checking for processes using E2E ports..."
          for port in 3201 4202 5436 6381; do
            pid=$(lsof -ti:$port 2>/dev/null || true)
            if [ ! -z "$pid" ]; then
              echo "⚠️ Found process $pid using port $port, terminating..."
              kill -9 $pid 2>/dev/null || true
              sleep 1
            fi
          done
          
          # Verify ports are free
          echo "✅ Port cleanup completed"
          netstat -tlnp | grep -E ":320[1-9]|:420[2-9]|:543[6-9]|:638[1-9]" || echo "No conflicting ports found"
          
          # Start containers and capture any startup failures
          if docker compose -f docker-compose.e2e.ci.yml up -d --build; then
            echo "✅ Docker containers started successfully"
          else
            echo "❌ Docker containers failed to start, capturing logs..."
            echo "📄 Backend container logs:"
            docker logs loyalty_backend_e2e 2>&1 || echo "No backend logs available"
            echo "📄 Frontend container logs:"
            docker logs loyalty_frontend_e2e 2>&1 || echo "No frontend logs available"
            echo "📄 PostgreSQL container logs:"
            docker logs loyalty_postgres_e2e 2>&1 || echo "No postgres logs available"
            echo "📄 Redis container logs:"
            docker logs loyalty_redis_e2e 2>&1 || echo "No redis logs available"
            echo "📊 Container status:"
            docker compose -f docker-compose.e2e.ci.yml ps
            exit 1
          fi
          
          # Wait for application to be ready
          echo "⏳ Waiting for isolated application to be ready..."
          
          # Wait for backend with simplified retry logic 
          echo "🔍 Waiting for backend to be ready..."
          BACKEND_READY=false
          for i in {1..60}; do
            echo "Attempt $i/60: Testing backend connection..."
            if curl -s http://localhost:4202/api/health >/dev/null 2>&1; then
              echo "✅ Backend is responding!"
              BACKEND_READY=true
              break
            fi
            echo "Backend not ready yet, waiting 5 seconds..."
            sleep 5
          done
          
          if [ "$BACKEND_READY" = false ]; then
            echo "❌ Backend failed to become ready after 5 minutes (60 attempts)"
            echo "🔍 Final connection test:"
            curl -v http://localhost:4202/api/health || echo "Final curl test failed"
            echo "📄 Backend container logs:"
            docker logs loyalty_backend_e2e 2>&1 || echo "No backend logs available"
            echo "📊 Container status:"
            docker compose -f docker-compose.e2e.ci.yml ps
            echo "🌐 Port connectivity test:"
            netstat -tlnp | grep :4202 || echo "Port 4202 not found in netstat"
            exit 1
          fi
          
          # Wait for frontend with simplified retry logic
          echo "🔍 Waiting for frontend to be ready..."
          FRONTEND_READY=false
          for i in {1..40}; do
            echo "Attempt $i/40: Testing frontend connection..."
            if curl -s http://localhost:3201 >/dev/null 2>&1; then
              echo "✅ Frontend is responding!"
              FRONTEND_READY=true
              break
            fi
            echo "Frontend not ready yet, waiting 3 seconds..."
            sleep 3
          done
          
          if [ "$FRONTEND_READY" = false ]; then
            echo "❌ Frontend failed to become ready after 2 minutes (40 attempts)"
            echo "🔍 Final frontend connection test:"
            curl -v http://localhost:3201 || echo "Final frontend curl test failed"
            echo "📄 Frontend container logs:"
            docker logs loyalty_frontend_e2e 2>&1 || echo "No frontend logs available"
            echo "📊 Container status:"
            docker compose -f docker-compose.e2e.ci.yml ps
            echo "🌐 Frontend port connectivity test:"
            netstat -tlnp | grep :3201 || echo "Port 3201 not found in netstat"
            exit 1
          fi
          
          echo "✅ Isolated application stack is ready for E2E testing"
          echo "🌐 Backend: http://localhost:4202/api/health"
          echo "🌐 Frontend: http://localhost:3201"

          # Network diagnostics: Check what interfaces backend is listening on
          echo ""
          echo "🔍 Network Diagnostics:"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

          echo "📡 Port 4202 listening status (host network):"
          ss -tlnp | grep :4202 || netstat -tlnp | grep :4202 || echo "⚠️ Port 4202 not found in host network"

          echo ""
          echo "🐳 Backend container network info:"
          docker exec loyalty_backend_e2e sh -c 'echo "Inside container - listening ports:" && netstat -tlnp 2>/dev/null || ss -tlnp' | grep -E "4202|State" || echo "⚠️ Could not get container network info"

          echo ""
          echo "🌐 Testing connectivity from different contexts:"
          echo "  Host curl to localhost:4202:"
          curl -s -o /dev/null -w "    Status: %{http_code}, Time: %{time_total}s\n" http://localhost:4202/api/health || echo "    ❌ Failed"

          echo "  Host curl to 127.0.0.1:4202:"
          curl -s -o /dev/null -w "    Status: %{http_code}, Time: %{time_total}s\n" http://127.0.0.1:4202/api/health || echo "    ❌ Failed"

          echo "  Host curl to 0.0.0.0:4202:"
          curl -s -o /dev/null -w "    Status: %{http_code}, Time: %{time_total}s\n" http://0.0.0.0:4202/api/health || echo "    ❌ Failed"

          echo ""
          echo "🔧 Backend container inspection:"
          echo "  Container IP (if bridged):"
          docker inspect loyalty_backend_e2e --format='    {{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}' || echo "    N/A (host network mode)"

          echo "  Network mode:"
          docker inspect loyalty_backend_e2e --format='    {{.HostConfig.NetworkMode}}'

          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""

      - name: "🎭 Execute Playwright E2E test suite against isolated stack"
        id: run-e2e-tests
        env:
          NODE_ENV: test
          DATABASE_URL: postgresql://loyalty:loyalty_password@localhost:5436/loyalty_db
          REDIS_URL: redis://localhost:6381
          BACKEND_URL: http://localhost:4202
          FRONTEND_URL: http://localhost:3201
          SKIP_PLAYWRIGHT_DOCKER_SETUP: "true"
          # GitHub Secrets for CI/CD environment validation test
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
          JWT_REFRESH_SECRET: ${{ secrets.JWT_REFRESH_SECRET }}
          SESSION_SECRET: ${{ secrets.SESSION_SECRET }}
          GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
          GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
          LINE_CHANNEL_ID: ${{ secrets.LINE_CHANNEL_ID }}
          LINE_CHANNEL_SECRET: ${{ secrets.LINE_CHANNEL_SECRET }}
        run: |
          echo "🎭 Running E2E tests with Playwright on isolated environment..."
          echo "🌐 Testing against Backend: http://localhost:4202"
          echo "🌐 Testing against Frontend: http://localhost:3201"

          # Verify Playwright is available
          if [ ! -d "node_modules/@playwright" ]; then
            echo "❌ @playwright/test not found in node_modules"
            echo "📂 Root node_modules contents:"
            ls -la node_modules/ 2>&1 | head -20 || echo "node_modules directory not found"
            exit 1
          fi

          echo "✅ @playwright/test found in node_modules"

          # Install Playwright browsers if needed
          echo "📥 Installing Playwright browsers..."
          npx playwright install --with-deps chromium

          # Verify Playwright installation
          echo "🔍 Verifying Playwright installation..."
          npx playwright --version

          # Playwright connectivity diagnostic
          echo ""
          echo "🧪 Playwright Connectivity Diagnostic:"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          node -e 'const http = require("http"); const urls = ["http://localhost:4202/api/health", "http://127.0.0.1:4202/api/health", "http://0.0.0.0:4202/api/health"]; console.log("Testing connectivity from Node.js (same context as Playwright):"); let completed = 0; urls.forEach(url => { const urlObj = new URL(url); const options = { hostname: urlObj.hostname, port: urlObj.port, path: urlObj.pathname, method: "GET", timeout: 5000 }; const req = http.request(options, (res) => { console.log(`  ✅ ${url}: HTTP ${res.statusCode}`); if (++completed === urls.length) process.exit(0); }); req.on("error", (e) => { console.log(`  ❌ ${url}: ${e.code} - ${e.message}`); if (++completed === urls.length) process.exit(0); }); req.on("timeout", () => { console.log(`  ⏱️ ${url}: Timeout`); req.destroy(); if (++completed === urls.length) process.exit(0); }); req.end(); }); setTimeout(() => process.exit(0), 7000);' || echo "⚠️ Node.js connectivity test failed"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""

          # CRITICAL: Verify containers are still running RIGHT before test execution
          echo "🔍 Pre-Test Container Status Check:"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          docker compose -f docker-compose.e2e.ci.yml ps
          echo ""
          echo "📋 Backend logs (last 30 lines):"
          docker compose -f docker-compose.e2e.ci.yml logs --tail=30 backend
          echo ""
          echo "🏥 Final health check before tests:"
          curl -f http://localhost:4202/api/health || echo "❌ Backend health check FAILED before test execution!"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""

          # Abort early if the isolated stack has disappeared
          RUNNING_SERVICES=$(docker compose -f docker-compose.e2e.ci.yml ps --services --filter "status=running" | tr '\n' ' ' | xargs)
          if [ -z "$RUNNING_SERVICES" ] || ! echo "$RUNNING_SERVICES" | grep -q "backend"; then
            echo "❌ Isolated E2E stack is not running. Capturing diagnostics before aborting..."
            echo "📊 docker compose ps --all"
            docker compose -f docker-compose.e2e.ci.yml ps -a || true
            echo "📦 docker ps -a (host)"
            docker ps -a || true
            echo "📄 Backend logs:"
            docker logs loyalty_backend_e2e 2>&1 || echo "No backend logs available"
            echo "📄 Frontend logs:"
            docker logs loyalty_frontend_e2e 2>&1 || echo "No frontend logs available"
            echo "📄 PostgreSQL logs:"
            docker logs loyalty_postgres_e2e 2>&1 || echo "No postgres logs available"
            echo "📄 Redis logs:"
            docker logs loyalty_redis_e2e 2>&1 || echo "No redis logs available"
            exit 1
          fi

          # Run all E2E tests (API + browser projects) against isolated environment (BLOCKING - must pass to succeed)
          echo "🎭 Running E2E tests (api + browser projects) - failures will BLOCK the pipeline..."
          npx playwright test --project=api --project=browser

      - name: "🗄️ Verify database migration rollback safety (non-blocking)"
        run: |
          echo "🗄️ Checking migration rollback safety..."
          if [ -f "./scripts/migration-rollback-safety.sh" ]; then
            ./scripts/migration-rollback-safety.sh check || echo "⚠️ Migration rollback check failed (non-blocking in CI)"
          else
            echo "⚠️ Migration rollback safety script not found - skipping"
          fi
      
      - name: "📊 Record E2E test results (success/failure status)"
        id: e2e-results
        if: always()
        run: |
          if [ "${{ steps.run-e2e-tests.outcome }}" = "success" ]; then
            echo "✅ E2E tests completed successfully"
            echo "passed=true" >> $GITHUB_OUTPUT
          else
            echo "❌ E2E tests failed"
            echo "passed=false" >> $GITHUB_OUTPUT
            exit 1
          fi

      - name: "📊 Write E2E test summary to job summary"
        if: always()
        run: |
          E2E_PASSED="${{ steps.e2e-results.outputs.passed }}"

          # Determine status
          if [ "$E2E_PASSED" == "true" ]; then
            E2E_STATUS="✅ Passed"
            STATUS_MSG="All E2E tests completed successfully!"
          else
            E2E_STATUS="❌ Failed"
            STATUS_MSG="Some E2E tests failed. Check the Allure report for details."
          fi

          echo "## 🎭 E2E Test Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Test Suite | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|:-----------|:-------|" >> $GITHUB_STEP_SUMMARY
          echo "| Playwright E2E Tests | $E2E_STATUS |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "> $STATUS_MSG" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Test artifacts info
          echo "## 📥 E2E Test Artifacts" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "E2E test results are available as workflow artifacts for 7 days." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Artifact | Description |" >> $GITHUB_STEP_SUMMARY
          echo "|:---------|:------------|" >> $GITHUB_STEP_SUMMARY
          echo "| \`allure-results-e2e-*\` | Allure test results for report generation |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Link to test reports page (use run_number for direct link)
          echo "## 🔗 View Reports" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "📊 [View Allure Report](https://jwinut.github.io/loyalty-app/test-reports/${{ github.run_number }}/)" >> $GITHUB_STEP_SUMMARY
          echo "📈 [View Coverage Report](https://jwinut.github.io/loyalty-app/test-reports/${{ github.run_number }}/coverage/lcov-report/)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

      - name: "📤 Upload E2E Allure results to GitHub artifacts"
        if: always()
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5
        with:
          name: allure-results-e2e-${{ github.run_id }}
          path: allure-results/
          retention-days: 7
          if-no-files-found: warn

      - name: "💬 Post E2E test results comment on pull request"
        if: always() && github.event_name == 'pull_request'
        uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd # v8
        continue-on-error: true
        with:
          script: |
            const e2eResult = '${{ steps.run-e2e-tests.outcome }}';
            const e2ePassed = e2eResult === 'success';

            const statusEmoji = e2ePassed ? '✅' : '❌';
            const statusText = e2ePassed ? 'Passed' : 'Failed';

            const comment = [
              '## 🧪 CI Test Results',
              '',
              '| Stage | Status |',
              '|-------|--------|',
              '| Unit Tests | ✅ Passed |',
              '| Integration Tests | ✅ Passed |',
              `| E2E Tests | ${statusEmoji} ${statusText} |`,
              '',
              '### 🎭 E2E Details',
              '| Test Suite | Status |',
              '|:-----------|:-------|',
              `| Playwright E2E | ${statusEmoji} ${statusText} |`,
              '',
              e2ePassed
                ? '> ✅ All tests passed! Ready for build and deployment.'
                : '> ❌ E2E tests failed - review test results.',
              '',
              `> Commit: \`${context.sha.substring(0, 7)}\` | [View Artifacts](https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId})`,
              '',
              '📊 [View Allure Report](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})',
            ].join('\n');

            const COMMENT_MARKER = '<!-- CI-TEST-RESULTS-COMMENT -->';
            const fullComment = COMMENT_MARKER + '\n' + comment;

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });

            const botComment = comments.find(c =>
              c.user.type === 'Bot' && c.body.includes(COMMENT_MARKER)
            );

            if (botComment) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: botComment.id,
                body: fullComment
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: fullComment
              });
            }

      # =============================================================================
      # PERFORMANCE TESTS (after E2E, reusing containers)
      # =============================================================================
      - name: "📦 Install k6 for load testing (if needed)"
        if: steps.run-e2e-tests.outcome == 'success'
        run: |
          if ! command -v k6 &> /dev/null; then
            echo "📦 Installing k6..."
            sudo gpg -k
            sudo gpg --no-default-keyring --keyring /usr/share/keyrings/k6-archive-keyring.gpg \
              --keyserver hkp://keyserver.ubuntu.com:80 \
              --recv-keys C5AD17C747E3415A3642D57D77C6C491D6AC1D69
            echo "deb [signed-by=/usr/share/keyrings/k6-archive-keyring.gpg] https://dl.k6.io/deb stable main" \
              | sudo tee /etc/apt/sources.list.d/k6.list
            sudo apt-get update && sudo apt-get install k6 -y
          else
            echo "✅ k6 already installed: $(k6 version)"
          fi

      - name: "⚡ Run performance tests"
        id: perf-tests
        if: steps.run-e2e-tests.outcome == 'success'
        continue-on-error: true
        env:
          DATABASE_URL: postgresql://loyalty:loyalty_password@localhost:5436/loyalty_db
          BACKEND_URL: http://localhost:4202
        working-directory: ./backend
        run: |
          mkdir -p benchmark-results

          echo "🧪 Running database benchmarks..."
          npm run test:perf:db || echo "⚠️ DB benchmarks had failures"

          echo "🧪 Running API benchmarks..."
          npm run test:perf:api || echo "⚠️ API benchmarks had failures"

          echo "🚀 Running k6 load tests..."
          k6 run src/__tests__/performance/k6/load-test.js \
            --out json=benchmark-results/k6-results.json || echo "⚠️ k6 tests had failures"

      - name: "📤 Upload performance benchmark results"
        if: always() && steps.perf-tests.outcome != 'skipped'
        uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4
        with:
          name: benchmark-results-${{ github.run_id }}
          path: backend/benchmark-results/
          retention-days: 30
          if-no-files-found: warn

      - name: "🧹 Teardown E2E Docker containers, volumes & generated config"
        if: always()
        run: |
          echo "🧹 Cleaning up isolated E2E environment..."
          docker compose -f docker-compose.e2e.ci.yml down -v --remove-orphans || true

          # SAFETY: Only remove test-specific volumes, NEVER docker system prune
          # docker system prune can destroy production containers/volumes!
          echo "🧹 Removing E2E volumes to prevent future migration state issues..."
          docker volume rm loyalty_postgres_e2e_data loyalty_redis_e2e_data 2>/dev/null || true
          docker volume rm loyalty-e2e-tests_loyalty_postgres_e2e_data loyalty-e2e-tests_loyalty_redis_e2e_data 2>/dev/null || true

          rm -f docker-compose.e2e.ci.yml || true
          echo "✅ Isolated E2E environment cleaned up"

  # =============================================================================
  # PHASE 2: BUILD & DEPLOYMENT PREPARATION (2-3 minutes, only on main)
  # =============================================================================
  
  # Job 2A: Build validation (only for main branch, after all tests)
  build-validation:
    name: "🏗️ Build Validation & Docker Images"
    runs-on: self-hosted
    needs: [validate-and-test, e2e-tests]
    if: github.ref == 'refs/heads/main' && always() && needs.validate-and-test.outputs.security-passed == 'true' && needs.validate-and-test.outputs.unit-tests-passed == 'true' && needs.validate-and-test.outputs.integration-tests-passed == 'true' && needs.e2e-tests.outputs.e2e-passed == 'true'
    timeout-minutes: 15
    outputs:
      build-passed: ${{ steps.build-results.outputs.passed }}
      backend-image: ${{ steps.docker-build.outputs.backend_image }}
      frontend-image: ${{ steps.docker-build.outputs.frontend_image }}
      images-ready: ${{ steps.docker-build.outputs.images_ready }}
    
    steps:
      - name: "🧹 Clean workspace, setup cache directories & checkout code"
        run: |
          echo "🧹 Cleaning workspace at ${{ github.workspace }}..."
          cd ${{ github.workspace }}

          # Force clean problematic directories with sudo
          sudo rm -rf frontend/dist backend/dist frontend/build backend/build 2>/dev/null || true
          sudo rm -rf frontend/.next backend/.next 2>/dev/null || true
          sudo rm -rf node_modules frontend/node_modules backend/node_modules 2>/dev/null || true

          # Clean git repository state
          if [ -d ".git" ]; then
            sudo git clean -xffd 2>/dev/null || true
            git reset --hard 2>/dev/null || true
          fi

          # Fix ownership
          sudo chown -R $USER:$USER . 2>/dev/null || true

          # Setup cache directories
          mkdir -p /tmp/runner-cache/${{ env.CACHE_VERSION }}/{npm,backend-node_modules,frontend-node_modules}
          echo "✅ Workspace cleaned and cache directories prepared"

      - name: "📥 Checkout code"
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6
        with:
          fetch-depth: 1
          clean: false

      - name: "⚡ Setup Node.js"
        uses: actions/setup-node@395ad3262231945c25e8478fd5baf05154b1d79f # v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: "📦 Restore/install backend & frontend dependencies from cache"
        run: |
          # Verify Node.js version
          echo "🔍 Node.js: $(node --version) | npm: $(npm --version)"
          NODE_MAJOR=$(node --version | cut -d'.' -f1 | tr -d 'v')
          if [ "$NODE_MAJOR" -lt "${{ env.NODE_VERSION }}" ]; then
            echo "❌ Node.js version mismatch! Expected ${{ env.NODE_VERSION }}.x, got $(node --version)"
            exit 1
          fi

          # Backend dependencies
          BACKEND_CACHE="/tmp/runner-cache/${{ env.CACHE_VERSION }}/backend-node_modules"
          if [ -d "$BACKEND_CACHE/node_modules" ]; then
            echo "✅ Restoring backend node_modules from cache..."
            cp -r "$BACKEND_CACHE/node_modules" ./backend/
          else
            echo "📦 Installing backend dependencies..."
            cd backend && npm ci --cache /tmp/runner-cache/${{ env.CACHE_VERSION }}/npm --prefer-offline --include=dev
            flock -w 60 "$BACKEND_CACHE/.flock" bash -c "rm -rf '$BACKEND_CACHE/node_modules' && cp -r node_modules '$BACKEND_CACHE/'"
            cd ..
          fi

          # Frontend dependencies
          FRONTEND_CACHE="/tmp/runner-cache/${{ env.CACHE_VERSION }}/frontend-node_modules"
          if [ -d "$FRONTEND_CACHE/node_modules" ]; then
            echo "✅ Restoring frontend node_modules from cache..."
            cp -r "$FRONTEND_CACHE/node_modules" ./frontend/
          else
            echo "📦 Installing frontend dependencies..."
            cd frontend && npm ci --cache /tmp/runner-cache/${{ env.CACHE_VERSION }}/npm --prefer-offline
            flock -w 60 "$FRONTEND_CACHE/.flock" bash -c "rm -rf '$FRONTEND_CACHE/node_modules' && cp -r node_modules '$FRONTEND_CACHE/'"
            cd ..
          fi
          echo "✅ All dependencies ready"

      - name: "🔧 Generate Prisma client & build backend/frontend sequentially"
        run: |
          echo "🔧 Generating Prisma client..."
          cd backend && npm run db:generate && cd ..

          echo "🏗️ Building backend (production)..."
          cd backend && npm run build:prod && cd ..

          echo "🏗️ Building frontend..."
          cd frontend && npm run build && cd ..

          echo "✅ Prisma generated and both applications built successfully"

      - name: "✅ Verify build artifacts (backend/frontend dist directories)"
        run: |
          echo "🔍 Verifying build outputs..."
          [ -d "backend/dist" ] || { echo "❌ backend/dist missing"; exit 1; }
          [ -d "frontend/dist" ] || { echo "❌ frontend/dist missing"; exit 1; }
          [ -f "backend/dist/index.js" ] || { echo "❌ backend/dist/index.js missing"; exit 1; }
          [ -f "frontend/dist/index.html" ] || { echo "❌ frontend/dist/index.html missing"; exit 1; }
          echo "✅ All build artifacts verified"
      
      - name: "🐳 Build production Docker images & tag with commit SHA"
        id: docker-build
        env:
          # Provide dummy environment variables for build-time validation
          # Real secrets will be used in production-deployment job
          NODE_ENV: production
          LOG_LEVEL: info
          CORS_ORIGINS: "https://loyalty.saichon.com"
          JWT_SECRET: "dummy-build-validation-secret-must-be-64-chars-minimum-xxxxxx"
          JWT_REFRESH_SECRET: "dummy-build-validation-refresh-secret-must-be-64-chars-minimum-xx"
          SESSION_SECRET: "dummy-build-validation-session-secret-min-32-chars"
          DATABASE_URL: "postgresql://dummy:dummy@dummy:5432/dummy"
          REDIS_URL: "redis://redis:6379"
          FRONTEND_URL: "https://loyalty.saichon.com"
          BACKEND_URL: "https://loyalty.saichon.com/api"
          VITE_API_URL: "https://loyalty.saichon.com/api"
          GOOGLE_CLIENT_ID: "dummy-google-client-id"
          GOOGLE_CLIENT_SECRET: "dummy-google-client-secret"
          GOOGLE_CALLBACK_URL: "https://loyalty.saichon.com/api/oauth/google/callback"
          FACEBOOK_APP_ID: "dummy-facebook-app-id"
          FACEBOOK_APP_SECRET: "dummy-facebook-app-secret"
          FACEBOOK_CALLBACK_URL: "https://loyalty.saichon.com/api/oauth/facebook/callback"
          LINE_CHANNEL_ID: "dummy-line-channel-id"
          LINE_CHANNEL_SECRET: "dummy-line-channel-secret"
          LINE_CALLBACK_URL: "https://loyalty.saichon.com/api/oauth/line/callback"
          TRANSLATION_FEATURE_ENABLED: "false"
          LOYALTY_USERNAME: "dummy-loyalty-user"
          LOYALTY_PASSWORD: "dummy-loyalty-password"
        run: |
          echo "🐳 Building production Docker images with BuildKit optimization..."

          # Enable BuildKit for faster builds
          export DOCKER_BUILDKIT=1
          export COMPOSE_DOCKER_CLI_BUILD=1
          export BUILDKIT_INLINE_CACHE=1

          # Validate Docker Compose configuration
          echo "🔍 Validating Docker Compose configuration..."
          docker compose -f docker-compose.yml -f docker-compose.prod.yml config > /dev/null
          echo "✅ Configuration validated"

          # Build images once with production target
          echo "🏗️ Building Docker images (production runner stage)..."
          docker compose -f docker-compose.yml -f docker-compose.prod.yml build \
            --build-arg BUILDKIT_INLINE_CACHE=1 \
            --parallel

          # Tag images with commit SHA for deployment tracking
          echo "🏷️ Tagging images with commit SHA..."
          COMMIT_SHA="${{ github.sha }}"
          SHORT_SHA="${COMMIT_SHA:0:7}"

          # Get image names from docker compose
          BACKEND_IMAGE=$(docker compose -f docker-compose.yml -f docker-compose.prod.yml images -q backend | head -1)
          FRONTEND_IMAGE=$(docker compose -f docker-compose.yml -f docker-compose.prod.yml images -q frontend | head -1)

          # If images were built, tag them
          if [ -n "$BACKEND_IMAGE" ]; then
            docker tag $BACKEND_IMAGE loyalty-app-backend:$COMMIT_SHA
            docker tag $BACKEND_IMAGE loyalty-app-backend:$SHORT_SHA
            docker tag $BACKEND_IMAGE loyalty-app-backend:latest
            echo "✅ Backend image tagged: $COMMIT_SHA, $SHORT_SHA, latest"
          fi

          if [ -n "$FRONTEND_IMAGE" ]; then
            docker tag $FRONTEND_IMAGE loyalty-app-frontend:$COMMIT_SHA
            docker tag $FRONTEND_IMAGE loyalty-app-frontend:$SHORT_SHA
            docker tag $FRONTEND_IMAGE loyalty-app-frontend:latest
            echo "✅ Frontend image tagged: $COMMIT_SHA, $SHORT_SHA, latest"
          fi

          # Verify images exist
          echo "🔍 Verifying tagged images..."
          docker images | grep -E "loyalty-app-(backend|frontend)" | grep -E "$SHORT_SHA|latest"

          # Quick smoke test
          echo "🧪 Running smoke tests..."
          if docker run --rm loyalty-app-backend:$SHORT_SHA node --version >/dev/null 2>&1; then
            echo "✅ Backend image smoke test passed"
          else
            echo "⚠️ Backend smoke test failed (non-critical)"
          fi

          # Output image tags for deployment job
          echo "images_ready=true" >> $GITHUB_OUTPUT
          echo "backend_image=loyalty-app-backend:$COMMIT_SHA" >> $GITHUB_OUTPUT
          echo "frontend_image=loyalty-app-frontend:$COMMIT_SHA" >> $GITHUB_OUTPUT

          echo "✅ Docker images built, tagged, and ready for deployment"
          echo "📦 Backend: loyalty-app-backend:$COMMIT_SHA"
          echo "📦 Frontend: loyalty-app-frontend:$COMMIT_SHA"
      
      - name: "📊 Record build validation results (passed status)"
        id: build-results
        run: |
          echo "✅ Build validation completed successfully"
          echo "passed=true" >> $GITHUB_OUTPUT

  # =============================================================================
  # PHASE 3A: DEVELOPMENT DEPLOYMENT (only on develop branch, auto-deploy)
  # No approval needed - deploys to port 5001 for testing
  # =============================================================================

  development-deployment:
    name: "🧪 Development Deployment"
    runs-on: self-hosted
    environment: development
    needs: [validate-and-test]
    if: |
      github.ref == 'refs/heads/develop' &&
      needs.validate-and-test.outputs.security-passed == 'true' &&
      needs.validate-and-test.outputs.unit-tests-passed == 'true' &&
      needs.validate-and-test.outputs.integration-tests-passed == 'true'
    timeout-minutes: 10
    concurrency:
      group: dev-deployment
      cancel-in-progress: false  # Queue deployments - never cancel mid-deployment
    env:
      DEPLOY_PATH: /home/nut/loyalty-app-develop

    steps:
      - name: "📊 Pre-deployment validation"
        run: |
          echo "🔍 Development deployment checks..."
          echo "Branch: ${{ github.ref_name }}"
          echo "Commit: ${{ github.sha }}"
          echo "Security: ${{ needs.validate-and-test.outputs.security-passed }}"
          echo "Unit Tests: ${{ needs.validate-and-test.outputs.unit-tests-passed }}"
          echo "Integration Tests: ${{ needs.validate-and-test.outputs.integration-tests-passed }}"
          echo "✅ All checks passed - proceeding with development deployment"

      - name: "📥 Code deployment"
        run: |
          echo "📥 Deploying code to development environment..."

          DEPLOY_DIR="${DEPLOY_PATH:-/home/nut/loyalty-app-develop}"

          if [ -d "$DEPLOY_DIR/.git" ]; then
            echo "📥 Updating existing repository..."
            cd "$DEPLOY_DIR"

            # Show current state
            BEFORE_COMMIT=$(git rev-parse --short HEAD 2>/dev/null || echo "none")
            echo "📍 Current commit: $BEFORE_COMMIT"
            echo "🎯 Target commit:  ${{ github.sha }}"

            # Clean any local changes
            git clean -fd 2>/dev/null || true
            git reset --hard HEAD 2>/dev/null || true

            # Fetch and checkout the specific commit
            git fetch --depth 1 origin ${{ github.sha }}
            git checkout ${{ github.sha }}
          else
            echo "📦 Fresh clone..."
            mkdir -p "$(dirname "$DEPLOY_DIR")"
            git clone --depth 1 \
              https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git \
              "$DEPLOY_DIR"
            cd "$DEPLOY_DIR"
            git fetch --depth 1 origin ${{ github.sha }}
            git checkout ${{ github.sha }}
          fi

          # Verify checkout succeeded
          AFTER_COMMIT=$(git rev-parse HEAD)
          TARGET_SHA="${{ github.sha }}"

          if [ "$AFTER_COMMIT" != "$TARGET_SHA" ]; then
            echo "❌ ERROR: Checkout verification failed!"
            echo "   Expected: $TARGET_SHA"
            echo "   Got:      $AFTER_COMMIT"
            exit 1
          fi

          echo ""
          echo "✅ Code deployment completed"
          echo "   Deployed SHA: $AFTER_COMMIT"

      - name: "🔧 Environment configuration"
        env:
          # Secrets from 'development' environment (no DEV_ prefix needed)
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
          JWT_REFRESH_SECRET: ${{ secrets.JWT_REFRESH_SECRET }}
          SESSION_SECRET: ${{ secrets.SESSION_SECRET }}
          GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
          GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
          LINE_CHANNEL_ID: ${{ secrets.LINE_CHANNEL_ID }}
          LINE_CHANNEL_SECRET: ${{ secrets.LINE_CHANNEL_SECRET }}
        run: |
          echo "🔧 Configuring development environment..."

          DEPLOY_DIR="${DEPLOY_PATH:-/home/nut/loyalty-app-develop}"
          cd "$DEPLOY_DIR"

          # Create .env.development file for development with GitHub Secrets
          cat > .env.development << EOF
          # Development Environment - Auto-generated by GitHub Actions
          NODE_ENV=development

          # URLs (Development) - use deployed dev domain for CORS consistency
          FRONTEND_URL=https://loyalty-dev.saichon.com
          BACKEND_URL=https://loyalty-dev.saichon.com/api
          VITE_API_URL=/api
          VITE_TRANSLATION_ENABLED=false

          # Internal ports
          BACKEND_PORT=4000
          FRONTEND_PORT=3000

          # Database & Redis
          DATABASE_URL=postgresql://loyalty_dev:loyalty_dev_pass@postgres:5432/loyalty_dev_db
          REDIS_URL=redis://redis:6379

          # Security
          JWT_SECRET=${JWT_SECRET}
          JWT_REFRESH_SECRET=${JWT_REFRESH_SECRET}
          SESSION_SECRET=${SESSION_SECRET}

          # OAuth
          GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
          GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}
          GOOGLE_CALLBACK_URL=https://loyalty-dev.saichon.com/api/oauth/google/callback
          LINE_CHANNEL_ID=${LINE_CHANNEL_ID}
          LINE_CHANNEL_SECRET=${LINE_CHANNEL_SECRET}
          LINE_CALLBACK_URL=https://loyalty-dev.saichon.com/api/oauth/line/callback

          # Azure Translation
          TRANSLATION_FEATURE_ENABLED=false

          # Development settings
          LOG_LEVEL=debug
          CORS_ORIGINS=http://localhost:3000,http://localhost:5001,http://127.0.0.1:3000,http://127.0.0.1:5001,https://loyalty-dev.saichon.com

          # Admin credentials (development only)
          LOYALTY_USERNAME=admin@dev.local
          LOYALTY_PASSWORD=admin123
          EOF

          # docker compose always reads .env; symlink to reuse dev config
          ln -sf .env.development .env

          echo "✅ Development environment configured"

      - name: "🔍 Pre-deployment port check (dev)"
        run: |
          echo "🔍 Checking if required development ports are available..."

          DEPLOY_DIR="${DEPLOY_PATH:-/home/nut/loyalty-app-develop}"
          cd "$DEPLOY_DIR"

          # Development ports (from docker-compose.dev.yml)
          REQUIRED_PORTS=(5435 6380 5001)
          PORTS_IN_USE=()

          for port in "${REQUIRED_PORTS[@]}"; do
            if lsof -i ":$port" -sTCP:LISTEN -t >/dev/null 2>&1; then
              echo "⚠️ Port $port is in use:"
              lsof -i ":$port" -sTCP:LISTEN || true
              PORTS_IN_USE+=($port)
            else
              echo "✅ Port $port is available"
            fi
          done

          if [ ${#PORTS_IN_USE[@]} -gt 0 ]; then
            echo ""
            echo "⚠️ Ports in use: ${PORTS_IN_USE[*]}"
            echo "🛑 Stopping existing development containers..."
            docker compose -f docker-compose.yml -f docker-compose.dev.yml down --timeout 30 || true

            # Re-check ports after shutdown
            echo "🔍 Re-checking ports after shutdown..."
            sleep 5
            STILL_IN_USE=()
            for port in "${PORTS_IN_USE[@]}"; do
              if lsof -i ":$port" -sTCP:LISTEN -t >/dev/null 2>&1; then
                STILL_IN_USE+=($port)
              fi
            done

            if [ ${#STILL_IN_USE[@]} -gt 0 ]; then
              echo "❌ WARNING: Ports still in use after shutdown: ${STILL_IN_USE[*]}"
              echo "Continuing with deployment - may fail if ports cannot be freed"
            else
              echo "✅ All ports now available after shutdown"
            fi
          fi

          echo "✅ Port check complete"

      - name: "🐳 Deploy development services"
        run: |
          echo "🐳 Deploying development services on port 5001..."

          DEPLOY_DIR="${DEPLOY_PATH:-/home/nut/loyalty-app-develop}"
          cd "$DEPLOY_DIR"

          # Build and start with dev configuration
          echo "🏗️ Building and starting development services..."
          docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d --build

          echo "⏳ Waiting for services to start..."
          sleep 15

          echo "✅ Development services deployed"

      - name: "🗄️ Database migration"
        run: |
          echo "🗄️ Running database migrations on development..."

          DEPLOY_DIR="${DEPLOY_PATH:-/home/nut/loyalty-app-develop}"
          cd "$DEPLOY_DIR"

          # Wait for database to be ready
          echo "⏳ Waiting for database..."
          for i in {1..30}; do
            if docker compose -f docker-compose.yml -f docker-compose.dev.yml exec -T postgres pg_isready -U loyalty_dev -d loyalty_dev_db >/dev/null 2>&1; then
              echo "✅ Database is ready"
              break
            fi
            echo "Waiting for database... ($i/30)"
            sleep 2
          done

          # Check migration status before running
          echo "🔍 Checking current migration status..."
          docker compose -f docker-compose.yml -f docker-compose.dev.yml exec -T backend npx prisma migrate status || echo "⚠️ Could not check migration status"

          # Run migrations with retry
          echo "🔄 Running Prisma migrations..."
          for attempt in 1 2 3; do
            echo "🔄 Migration attempt $attempt/3..."

            if docker compose -f docker-compose.yml -f docker-compose.dev.yml exec -T backend npm run db:migrate:deploy; then
              echo "✅ Database migrations completed successfully"
              break
            else
              echo "❌ Migration attempt $attempt failed"

              if [ $attempt -eq 3 ]; then
                echo "🔧 Attempting baseline resolution for 0_init..."
                docker compose -f docker-compose.yml -f docker-compose.dev.yml exec -T backend npx prisma migrate resolve --applied 0_init 2>/dev/null || true

                echo "🔄 Final migration attempt after baseline..."
                if docker compose -f docker-compose.yml -f docker-compose.dev.yml exec -T backend npm run db:migrate:deploy; then
                  echo "✅ Database migrations completed after baseline resolution"
                else
                  echo "❌ All migration attempts failed"
                  docker compose -f docker-compose.yml -f docker-compose.dev.yml logs --tail=20 backend
                  exit 1
                fi
              else
                sleep 3
              fi
            fi
          done

          # Verify final migration status
          echo "🔍 Post-migration status:"
          docker compose -f docker-compose.yml -f docker-compose.dev.yml exec -T backend npx prisma migrate status || true

          echo "✅ Database migration phase completed"

      - name: "🔍 Verify database schema and stored procedures"
        run: |
          echo "🔍 Verifying database schema and stored procedures..."
          DEPLOY_DIR="${DEPLOY_PATH:-/home/nut/loyalty-app-develop}"
          cd "$DEPLOY_DIR"

          # Verify critical tables exist
          echo "📋 Checking critical tables..."
          TABLES_CHECK=$(docker compose -f docker-compose.yml -f docker-compose.dev.yml exec -T postgres psql -U loyalty -d loyalty_dev_db -t -c "
            SELECT string_agg(tablename, ',')
            FROM pg_tables
            WHERE schemaname = 'public'
            AND tablename IN ('users', 'coupons', 'user_coupons', 'tiers', 'user_loyalty', 'user_audit_log', 'surveys');
          " 2>/dev/null | tr -d ' \n')

          REQUIRED_TABLES="users,coupons,user_coupons,tiers,user_loyalty,user_audit_log,surveys"
          MISSING_TABLES=""
          for table in $(echo "$REQUIRED_TABLES" | tr ',' ' '); do
            if ! echo "$TABLES_CHECK" | grep -q "$table"; then
              MISSING_TABLES="$MISSING_TABLES $table"
            fi
          done

          if [ -n "$MISSING_TABLES" ]; then
            echo "❌ Missing tables:$MISSING_TABLES"
            exit 1
          else
            echo "✅ All critical tables exist"
          fi

          # Verify critical stored procedures exist
          echo "📋 Checking critical stored procedures..."
          FUNCTIONS_CHECK=$(docker compose -f docker-compose.yml -f docker-compose.dev.yml exec -T postgres psql -U loyalty -d loyalty_dev_db -t -c "
            SELECT string_agg(proname, ',')
            FROM pg_proc p
            JOIN pg_namespace n ON p.pronamespace = n.oid
            WHERE n.nspname = 'public'
            AND proname IN (
              'assign_coupon_to_user',
              'redeem_coupon',
              'award_points',
              'recalculate_user_tier_by_nights'
            );
          " 2>/dev/null | tr -d ' \n')

          REQUIRED_FUNCTIONS="assign_coupon_to_user,redeem_coupon,award_points,recalculate_user_tier_by_nights"
          MISSING_FUNCTIONS=""
          for func in $(echo "$REQUIRED_FUNCTIONS" | tr ',' ' '); do
            if ! echo "$FUNCTIONS_CHECK" | grep -q "$func"; then
              MISSING_FUNCTIONS="$MISSING_FUNCTIONS $func"
            fi
          done

          if [ -n "$MISSING_FUNCTIONS" ]; then
            echo "❌ Missing stored procedures:$MISSING_FUNCTIONS"
            echo "⚠️ Run migration to create missing stored procedures"
            exit 1
          else
            echo "✅ All critical stored procedures exist"
          fi

          # Verify critical triggers exist
          echo "📋 Checking critical triggers..."
          TRIGGERS_CHECK=$(docker compose -f docker-compose.yml -f docker-compose.dev.yml exec -T postgres psql -U loyalty -d loyalty_dev_db -t -c "
            SELECT string_agg(tgname, ',')
            FROM pg_trigger t
            JOIN pg_class c ON t.tgrelid = c.oid
            WHERE NOT t.tgisinternal
            AND tgname IN ('trigger_update_user_tier_on_nights', 'set_membership_id_trigger', 'audit_user_changes');
          " 2>/dev/null | tr -d ' \n')

          REQUIRED_TRIGGERS="trigger_update_user_tier_on_nights,set_membership_id_trigger,audit_user_changes"
          MISSING_TRIGGERS=""
          for trigger in $(echo "$REQUIRED_TRIGGERS" | tr ',' ' '); do
            if ! echo "$TRIGGERS_CHECK" | grep -q "$trigger"; then
              MISSING_TRIGGERS="$MISSING_TRIGGERS $trigger"
            fi
          done

          if [ -n "$MISSING_TRIGGERS" ]; then
            echo "⚠️ Missing triggers:$MISSING_TRIGGERS (may be optional)"
          else
            echo "✅ All critical triggers exist"
          fi

          echo "✅ Database schema verification completed"

      - name: "🏥 Health check"
        run: |
          echo "🏥 Checking development service health..."

          DEPLOY_DIR="${DEPLOY_PATH:-/home/nut/loyalty-app-develop}"

          # Check backend health
          for i in {1..30}; do
            if curl -f -s http://localhost:5001/api/health >/dev/null 2>&1; then
              echo "✅ Backend health check passed"
              break
            fi
            echo "Waiting for backend... ($i/30)"
            sleep 2
          done

          # Check if frontend is responding
          if curl -f -s http://localhost:5001 >/dev/null 2>&1; then
            echo "✅ Frontend health check passed"
          else
            echo "⚠️ Frontend may still be starting..."
          fi

          # Show running containers (run from deploy dir to find .env.development)
          echo ""
          echo "📊 Running development containers:"
          cd "$DEPLOY_DIR"
          docker compose -f docker-compose.yml -f docker-compose.dev.yml ps

          echo ""
          echo "✅ Development deployment completed successfully!"
          echo "🌐 Development URL: http://localhost:5001"

      - name: "🧹 Cleanup development env files"
        if: always()
        run: |
          DEPLOY_DIR="${DEPLOY_PATH:-/home/nut/loyalty-app-develop}"
          cd "$DEPLOY_DIR"
          rm -f .env .env.development || true

  # =============================================================================
  # PHASE 3B: PRODUCTION DEPLOYMENT (only on main, after all tests pass)
  # Pipeline Order: Pre-validation → Backup → Code Deploy → Dependencies →
  # Environment → Service Deploy → Migration → Health Check → Summary
  # =============================================================================

  production-shutdown-approval:
    name: "🛑 Confirm Production Deployment"
    runs-on: ubuntu-latest
    environment:
      name: production-shutdown
    needs: [build-validation]
    if: github.ref == 'refs/heads/main' && needs.build-validation.outputs.build-passed == 'true'
    steps:
      - name: "📋 Await manual approval"
        run: |
          echo "✅ Manual approval received for production deployment."

  production-deployment:
    name: "🚀 Production Deployment"
    runs-on: self-hosted
    environment: production
    needs: [build-validation, production-shutdown-approval]
    if: |
      github.ref == 'refs/heads/main' &&
      needs.build-validation.outputs.build-passed == 'true' &&
      needs.production-shutdown-approval.result == 'success'
    timeout-minutes: 15
    concurrency:
      group: prod-deployment
      cancel-in-progress: false  # Queue deployments - never cancel mid-deployment
    env:
      DEPLOY_PATH: /home/nut/loyalty-app-production

    steps:
      - name: "📊 Pre-deployment validation (quality gates & deployment directory)"
        id: validate-deploy-dir
        run: |
          echo "🔍 Pre-deployment checks..."
          echo "Build Validation: ${{ needs.build-validation.outputs.build-passed }}"
          echo "Backend Image: ${{ needs.build-validation.outputs.backend-image }}"
          echo "Frontend Image: ${{ needs.build-validation.outputs.frontend-image }}"
          echo "Images Ready: ${{ needs.build-validation.outputs.images-ready }}"

          if [ "${{ needs.build-validation.outputs.build-passed }}" != "true" ]; then
            echo "❌ Build validation failed - deployment blocked"
            exit 1
          fi
          echo "✅ All quality gates passed"

          echo ""
          echo "🔗 Validating deployment directory..."
          DEPLOY_DIR="${DEPLOY_PATH:-/home/nut/loyalty-app-production}"

          # Directory exists (bind mount working)
          if [ ! -d "$DEPLOY_DIR" ]; then
            echo "❌ FATAL: Deployment directory not found: $DEPLOY_DIR"
            echo "Verify runner's docker-compose.yml has bind mount configured"
            exit 1
          fi
          echo "✅ Directory exists: $DEPLOY_DIR"

          # It's a git repository
          if [ ! -d "$DEPLOY_DIR/.git" ]; then
            echo "❌ FATAL: Not a git repository: $DEPLOY_DIR"
            exit 1
          fi
          echo "✅ Valid git repository"

          cd "$DEPLOY_DIR"
          git config --global --add safe.directory "$DEPLOY_DIR" 2>/dev/null || true

          # Git commands work
          if ! git rev-parse HEAD >/dev/null 2>&1; then
            echo "❌ FATAL: Git commands failing in $DEPLOY_DIR"
            exit 1
          fi
          CURRENT_COMMIT=$(git rev-parse --short HEAD)
          echo "✅ Git working (current commit: $CURRENT_COMMIT)"

          # Remote is configured
          REMOTE_URL=$(git remote get-url origin 2>/dev/null || echo "")
          if [ -z "$REMOTE_URL" ]; then
            echo "❌ FATAL: No 'origin' remote configured"
            exit 1
          fi
          echo "✅ Remote configured: origin"

          # Can reach the target commit
          git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git
          if ! git fetch --depth 1 origin ${{ github.sha }} 2>/dev/null; then
            echo "⚠️ WARNING: Could not fetch commit - will retry during deployment"
          else
            echo "✅ Target commit is reachable"
          fi

          echo ""
          echo "📋 Ready for deployment: $DEPLOY_DIR ($CURRENT_COMMIT → ${{ github.sha }})"

      - name: "💾 Smart database backup (pre-shutdown)"
        id: backup
        run: |
          echo "💾 Creating smart database backup..."
          
          DEPLOY_DIR="${DEPLOY_PATH:-/home/nut/loyalty-app-production}"
          
          # Only backup if deployment directory exists
          if [ ! -d "$DEPLOY_DIR" ]; then
            echo "⚠️ First deployment - skipping backup"
            echo "status=skipped" >> $GITHUB_OUTPUT
            exit 0
          fi
          
          cd "$DEPLOY_DIR"
          mkdir -p backups
          
          # Quick backup with timeout
          timestamp=$(date +%Y%m%d_%H%M%S)
          backup_file="backups/pre_deploy_${timestamp}.sql"
          
          if timeout 60s docker exec loyalty_postgres_production pg_dump -U loyalty -d loyalty_db > "$backup_file" 2>/dev/null; then
            if [ -s "$backup_file" ]; then
              echo "✅ Backup created: $backup_file ($(du -h "$backup_file" | cut -f1))"
              echo "status=success" >> $GITHUB_OUTPUT
            else
              echo "⚠️ Backup failed - empty file"
              rm -f "$backup_file"
              echo "status=failed" >> $GITHUB_OUTPUT
            fi
          else
            echo "⚠️ Backup timed out or failed"
            rm -f "$backup_file" 2>/dev/null || true
            echo "status=failed" >> $GITHUB_OUTPUT
          fi
      
      - name: "📥 Optimized code deployment"
        run: |
          echo "📥 Deploying code..."

          DEPLOY_DIR="${DEPLOY_PATH:-/home/nut/loyalty-app-production}"
          cd "$DEPLOY_DIR"

          # Show current state
          BEFORE_COMMIT=$(git rev-parse --short HEAD)
          echo "📍 Current commit: $BEFORE_COMMIT"
          echo "🎯 Target commit:  ${{ github.sha }}"

          # Clean any local changes
          echo "🧹 Cleaning local changes..."
          git clean -fd 2>/dev/null || true
          git reset --hard HEAD 2>/dev/null || true

          # Fetch and checkout the specific commit (remote already configured in validation step)
          echo "📥 Fetching and checking out target commit..."
          git fetch --depth 1 origin ${{ github.sha }}
          git checkout ${{ github.sha }}

          # Verify checkout succeeded (use full SHA for robust verification)
          AFTER_COMMIT=$(git rev-parse HEAD)
          TARGET_SHA="${{ github.sha }}"

          if [ "$AFTER_COMMIT" != "$TARGET_SHA" ]; then
            echo "❌ ERROR: Checkout verification failed!"
            echo "   Expected: $TARGET_SHA"
            echo "   Got:      $AFTER_COMMIT"
            exit 1
          fi

          echo ""
          echo "✅ Code deployment completed"
          echo "   Before: $BEFORE_COMMIT"
          echo "   Deployed SHA: $AFTER_COMMIT"

      - name: "⚡ Setup Node.js"
        uses: actions/setup-node@395ad3262231945c25e8478fd5baf05154b1d79f # v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: "⚡ Lightning-fast dependency setup"
        run: |
          # Verify Node.js version
          echo "🔍 Node.js: $(node --version) | npm: $(npm --version)"
          NODE_MAJOR=$(node --version | cut -d'.' -f1 | tr -d 'v')
          if [ "$NODE_MAJOR" -lt "${{ env.NODE_VERSION }}" ]; then
            echo "❌ Node.js version mismatch! Expected ${{ env.NODE_VERSION }}.x, got $(node --version)"
            exit 1
          fi

          echo "⚡ Setting up dependencies with maximum speed..."

          DEPLOY_DIR="${DEPLOY_PATH:-/home/nut/loyalty-app-production}"
          cd "$DEPLOY_DIR"

          # Configure npm for maximum performance
          # Use cache inside deployment directory (bind-mounted from host)
          export NPM_CONFIG_CACHE="$DEPLOY_DIR/.npm-cache"
          export NPM_CONFIG_PREFER_OFFLINE=true
          export NPM_CONFIG_AUDIT=false
          export NPM_CONFIG_FUND=false
          mkdir -p "$NPM_CONFIG_CACHE"
          
          # Parallel dependency installation with smart caching
          install_deps() {
            local service=$1
            local dir=$2
            echo "Installing $service dependencies..."
            cd "$DEPLOY_DIR/$dir"
            
            # Use npm ci for reproducible installs with integrity verification
            # Strictly follows package-lock.json and verifies integrity hashes
            npm ci --prefer-offline --no-audit --ignore-scripts || {
              echo "❌ npm ci failed for $service"
              echo "   Ensure package-lock.json is committed and in sync with package.json"
              echo "   Regenerate lock file locally and commit the update"
              exit 1
            }
            echo "✅ $service dependencies installed (integrity verified)"
          }
          
          # Install in parallel
          install_deps "backend" "backend" &
          install_deps "frontend" "frontend" &
          wait
          
          echo "✅ Dependencies installed in parallel"
      
      - name: "🔧 Validate secrets & create .env file for Docker Compose"
        run: |
          echo "🔧 Configuring production environment..."

          # Validate critical secrets
          missing_secrets=()
          [ -z "${{ secrets.JWT_SECRET }}" ] && missing_secrets+=("JWT_SECRET")
          [ -z "${{ secrets.DATABASE_URL }}" ] && missing_secrets+=("DATABASE_URL")

          if [ ${#missing_secrets[@]} -ne 0 ]; then
            echo "❌ Missing secrets: ${missing_secrets[*]}"
            exit 1
          fi
          echo "✅ Critical secrets validated"

          echo ""
          echo "📝 Creating .env file..."
          DEPLOY_DIR="${DEPLOY_PATH:-/home/nut/loyalty-app-production}"
          cd "$DEPLOY_DIR"

          cat > .env << EOF
          NODE_ENV=production
          LOG_LEVEL=info
          CORS_ORIGINS=${{ vars.CORS_ORIGINS }}

          JWT_SECRET=${{ secrets.JWT_SECRET }}
          JWT_REFRESH_SECRET=${{ secrets.JWT_REFRESH_SECRET }}
          SESSION_SECRET=${{ secrets.SESSION_SECRET }}
          DATABASE_URL=${{ secrets.DATABASE_URL }}
          REDIS_URL=${{ vars.REDIS_URL || 'redis://redis:6379' }}
          FRONTEND_URL=${{ vars.FRONTEND_URL }}
          BACKEND_URL=${{ vars.BACKEND_URL }}
          VITE_API_URL=${{ vars.VITE_API_URL }}
          VITE_TRANSLATION_ENABLED=false
          TRANSLATION_FEATURE_ENABLED=false

          GOOGLE_CLIENT_ID=${{ secrets.GOOGLE_CLIENT_ID }}
          GOOGLE_CLIENT_SECRET=${{ secrets.GOOGLE_CLIENT_SECRET }}
          GOOGLE_CALLBACK_URL=${{ vars.GOOGLE_CALLBACK_URL }}
          LINE_CHANNEL_ID=${{ secrets.LINE_CHANNEL_ID }}
          LINE_CHANNEL_SECRET=${{ secrets.LINE_CHANNEL_SECRET }}
          LINE_CALLBACK_URL=${{ vars.LINE_CALLBACK_URL }}

          LOYALTY_USERNAME=${{ secrets.LOYALTY_USERNAME }}
          LOYALTY_PASSWORD=${{ secrets.LOYALTY_PASSWORD }}
          EOF

          echo "✅ .env file created ($(wc -l < .env) lines)"

          # Validate .env file (without exposing secrets)
          echo "🔍 Validation: NODE_ENV=$(grep '^NODE_ENV=' .env | cut -d'=' -f2)"
          empty_vars=$(grep '=$' .env | cut -d'=' -f1 | head -5 || true)
          if [ -n "$empty_vars" ]; then
            echo "⚠️ Empty environment variables detected: $empty_vars"
            echo "   This may cause issues - please check GitHub Secrets/Variables"
          else
            echo "✅ All environment variables have values"
          fi
      
      - name: "🛡️ Stop conflicting dev containers & verify production ports available"
        run: |
          echo "🛡️ Checking for dev containers that might block production ports..."

          # Stop dev containers that could conflict
          DEV_CONTAINERS=("loyalty_postgres_dev" "loyalty_redis_dev" "loyalty_nginx_dev" "loyalty_backend_dev" "loyalty_frontend_dev")
          STOPPED_CONTAINERS=()
          for container in "${DEV_CONTAINERS[@]}"; do
            if docker ps -a --format '{{.Names}}' | grep -q "^${container}$"; then
              echo "⚠️ Stopping dev container: $container"
              docker stop "$container" 2>/dev/null || true
              docker rm "$container" 2>/dev/null || true
              STOPPED_CONTAINERS+=("$container")
            fi
          done
          [ ${#STOPPED_CONTAINERS[@]} -gt 0 ] && echo "🧹 Removed: ${STOPPED_CONTAINERS[*]}" && sleep 5

          echo ""
          echo "🔍 Checking production ports (5434, 6379, 4001)..."
          DEPLOY_DIR="${DEPLOY_PATH:-/home/nut/loyalty-app-production}"
          cd "$DEPLOY_DIR"

          REQUIRED_PORTS=(5434 6379 4001)
          PORTS_IN_USE=()
          for port in "${REQUIRED_PORTS[@]}"; do
            if lsof -i ":$port" -sTCP:LISTEN -t >/dev/null 2>&1; then
              echo "⚠️ Port $port in use"
              PORTS_IN_USE+=($port)
            else
              echo "✅ Port $port available"
            fi
          done

          if [ ${#PORTS_IN_USE[@]} -gt 0 ]; then
            echo "🛑 Attempting graceful shutdown..."
            docker compose -f docker-compose.yml -f docker-compose.prod.yml down --timeout 30 || true
            sleep 5

            # Re-check ports
            STILL_IN_USE=()
            for port in "${PORTS_IN_USE[@]}"; do
              lsof -i ":$port" -sTCP:LISTEN -t >/dev/null 2>&1 && STILL_IN_USE+=($port)
            done

            if [ ${#STILL_IN_USE[@]} -gt 0 ]; then
              echo "❌ FATAL: Ports still in use: ${STILL_IN_USE[*]}"
              exit 1
            fi
            echo "✅ Ports freed after shutdown"
          fi
          echo "✅ All production ports available"

      - name: "🔒 Validate production configuration"
        run: |
          echo "🔒 Validating production configuration..."

          DEPLOY_DIR="${DEPLOY_PATH:-/home/nut/loyalty-app-production}"
          cd "$DEPLOY_DIR"

          HAS_ERROR=0

          # Check 1: Verify no dev commands in production config
          echo "📋 Checking for dev commands in production config..."

          # Get merged config as YAML
          MERGED_CONFIG=$(docker compose -f docker-compose.yml -f docker-compose.prod.yml config 2>/dev/null)

          # Extract backend command using yq-style parsing (grep between backend: and next service)
          # Look for command: in the backend service section
          BACKEND_SECTION=$(echo "$MERGED_CONFIG" | sed -n '/^  backend:/,/^  [a-z]/p' | head -n -1)
          BACKEND_CMD=$(echo "$BACKEND_SECTION" | grep "command:" | head -1 || echo "")

          echo "   Backend command config: ${BACKEND_CMD:-'(none - using Dockerfile CMD)'}"

          if echo "$BACKEND_CMD" | grep -qE "(npm run dev|npx tsx|nodemon|ts-node)"; then
            echo "❌ FATAL: Development command found in backend service!"
            echo "   Found: $BACKEND_CMD"
            echo "   Production must use Dockerfile CMD: node dist/index.js"
            HAS_ERROR=1
          else
            echo "✅ Backend command OK (using Dockerfile CMD or explicit production command)"
          fi

          # Also check base docker-compose.yml directly for dev commands that might leak
          echo "📋 Verifying base docker-compose.yml doesn't have dev commands..."
          BASE_BACKEND_CMD=$(grep -A30 "^  backend:" docker-compose.yml | grep "command:" | head -1 || echo "")
          if echo "$BASE_BACKEND_CMD" | grep -qE "(npm run dev|npx tsx|nodemon|ts-node)"; then
            echo "❌ FATAL: Development command in base docker-compose.yml!"
            echo "   Found: $BASE_BACKEND_CMD"
            echo "   Dev commands must be in docker-compose.dev.yml only"
            HAS_ERROR=1
          else
            echo "✅ Base docker-compose.yml OK (no dev commands)"
          fi

          # Check 2: Verify backend uses runner target (production stage)
          BACKEND_TARGET=$(echo "$MERGED_CONFIG" | grep -A20 "backend:" | grep -A10 "build:" | grep "target:" | head -1 || echo "")
          if echo "$BACKEND_TARGET" | grep -q "development"; then
            echo "❌ FATAL: Backend using development Docker target!"
            echo "   Found: $BACKEND_TARGET"
            echo "   Production must use: target: runner"
            HAS_ERROR=1
          else
            echo "✅ Backend Docker target OK"
          fi

          # Check 3: Verify NODE_ENV is production
          NODE_ENV_VALUE=$(echo "$MERGED_CONFIG" | grep -A100 "backend:" | grep "NODE_ENV:" | head -1 || echo "")
          if echo "$NODE_ENV_VALUE" | grep -qE "(development|dev)"; then
            echo "❌ FATAL: NODE_ENV not set to production!"
            echo "   Found: $NODE_ENV_VALUE"
            HAS_ERROR=1
          else
            echo "✅ NODE_ENV OK"
          fi

          if [ $HAS_ERROR -eq 1 ]; then
            echo ""
            echo "🛑 Production deployment blocked due to configuration errors."
            echo "   Please fix docker-compose.prod.yml before deploying."
            exit 1
          fi

          echo "✅ Production configuration validated"

      - name: "🚀 Optimized service deployment"
        run: |
          echo "🚀 Deploying services with zero-downtime strategy..."

          DEPLOY_DIR="${DEPLOY_PATH:-/home/nut/loyalty-app-production}"
          cd "$DEPLOY_DIR"

          # Enable BuildKit for faster builds
          export DOCKER_BUILDKIT=1
          export COMPOSE_DOCKER_CLI_BUILD=1
          
          # Build and start services
          # Dockerfiles are optimized for caching:
          # - Dependencies cached until package*.json changes
          # - Config files cached until they change
          # - Source code copied LAST (only build layer rebuilds on code changes)
          echo "🏗️ Building and starting services..."
          docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
          
          echo "✅ Services deployed"
          
          # Give services time to initialize before health checks
          echo "⏳ Allowing services to initialize (30 seconds)..."
          sleep 30
          
          # Quick startup verification
          echo "🔍 Quick startup verification:"
          docker compose ps
          echo "📝 Backend startup logs (last 10 lines):"
          docker compose logs --tail=10 backend 2>/dev/null || echo "No backend logs yet"
      
      - name: "🗃️ Database migration (container context)"
        run: |
          echo "🗃️ Running database migrations with enhanced logging..."
          
          DEPLOY_DIR="${DEPLOY_PATH:-/home/nut/loyalty-app-production}"
          cd "$DEPLOY_DIR"
          
          # Pre-migration container status
          echo "📦 Pre-migration container status:"
          docker compose ps
          
          # Wait for database to be ready with enhanced logging
          echo "💾 Waiting for database to be ready..."
          if timeout 60s bash -c 'until docker compose exec -T postgres pg_isready -U loyalty -d loyalty_db; do echo "Database not ready, waiting..."; sleep 2; done'; then
            echo "✅ Database is ready"
          else
            echo "❌ Database readiness timeout"
            echo "📄 Database logs:"
            docker compose logs --tail=20 postgres
            exit 1
          fi
          
          # Wait for backend container to be fully ready
          echo "📦 Waiting for backend container to be ready..."
          if timeout 60s bash -c 'until docker compose exec -T backend echo "ready" >/dev/null 2>&1; do echo "Backend not ready, waiting..."; sleep 2; done'; then
            echo "✅ Backend container is ready"
          else
            echo "❌ Backend container timeout"
            echo "📄 Backend logs:"
            docker compose logs --tail=20 backend
            exit 1
          fi
          
          # Give containers additional time to stabilize
          echo "⏳ Allowing containers to stabilize (10 seconds)..."
          sleep 10
          
          # Show database connection info before migration
          echo "🔗 Database connection check:"
          docker compose exec -T backend node -e "console.log('DATABASE_URL:', process.env.DATABASE_URL ? 'configured' : 'missing')" || echo "Cannot check DB config"
          
          # Run migration from within backend container (correct network context)
          echo "🗃️ Running database migration from container..."
          for attempt in 1 2 3; do
            echo "\n🔄 Migration attempt $attempt/3..."
            
            # First check migration status
            echo "🔍 Checking current migration status:"
            docker compose exec -T backend npm run db:migrate:status || echo "Cannot check migration status"
            
            if docker compose exec -T backend npm run db:migrate:deploy; then
              echo "✅ Database migrations completed successfully"
              
              # Verify migration success
              echo "🔍 Post-migration verification:"
              docker compose exec -T backend npm run db:migrate:status || echo "Cannot verify final status"
              break
            else
              echo "❌ Migration attempt $attempt failed"
              
              # Show backend logs for debugging
              echo "📄 Backend logs during migration failure:"
              docker compose logs --tail=10 backend
              
              if [ $attempt -eq 3 ]; then
                echo "❌ Migration failed after 3 attempts"
                
                # Enhanced baseline scenario handling
                echo "🔍 Checking for baseline scenario..."
                migration_status=$(docker compose exec -T backend npm run db:migrate:status 2>&1 || echo "status_check_failed")
                echo "Migration status output: $migration_status"
                
                if echo "$migration_status" | grep -q "migrations have not yet been applied"; then
                  echo "🔧 Attempting baseline resolution..."
                  docker compose exec -T backend npx prisma migrate resolve --applied 0_init 2>/dev/null || echo "Baseline resolution failed"
                  
                  echo "🔄 Retrying migration after baseline..."
                  if docker compose exec -T backend npm run db:migrate:deploy; then
                    echo "✅ Database migrations completed after baseline resolution"
                    break
                  else
                    echo "❌ Migration still failed after baseline"
                  fi
                fi
                
                # Final failure diagnostics
                echo "🔴 MIGRATION FAILURE DIAGNOSTICS:"
                echo "=== Container Status ==="
                docker compose ps
                echo "\n=== Database Connection Test ==="
                docker compose exec -T postgres pg_isready -U loyalty -d loyalty_db || echo "Database connection failed"
                echo "\n=== Backend Environment ==="
                docker compose exec -T backend env | grep -E "DATABASE_URL|NODE_ENV" || echo "Cannot check environment"
                echo "\n=== Backend Logs (last 30 lines) ==="
                docker compose logs --tail=30 backend
                
                echo "❌ All migration attempts failed"
                exit 1
              else
                echo "⏳ Retrying in 5 seconds..."
                sleep 5
              fi
            fi
          done
          
          echo "✅ Database migration phase completed"
      
      - name: "🔐🗄️ Post-deployment OAuth & database validation"
        run: |
          echo "🔐 Validating OAuth and database in production..."
          DEPLOY_DIR="${DEPLOY_PATH:-/home/nut/loyalty-app-production}"
          cd "$DEPLOY_DIR"

          # Wait for application to be ready
          sleep 10

          # OAuth validation
          echo "🔍 Running OAuth validation..."
          if docker compose exec -T backend test -f "/app/scripts/validate-oauth-health.sh"; then
            if docker compose exec -T backend bash -c "cd /app && ./scripts/validate-oauth-health.sh"; then
              echo "✅ OAuth validation passed"
            else
              echo "⚠️ OAuth validation failed - check configuration"
              docker compose logs --tail=10 backend
            fi
          else
            echo "⚠️ OAuth script not found - skipping"
          fi

          # Database migration status
          echo ""
          echo "🗄️ Checking database migration status..."
          docker compose exec -T backend npm run db:migrate:status && echo "✅ Migration status OK" || echo "⚠️ Cannot verify migration status"

          # Verify critical tables exist
          echo ""
          echo "📋 Verifying critical database tables..."
          TABLES_CHECK=$(docker compose exec -T postgres psql -U loyalty -d loyalty_db -t -c "
            SELECT string_agg(tablename, ',')
            FROM pg_tables
            WHERE schemaname = 'public'
            AND tablename IN ('users', 'coupons', 'user_coupons', 'tiers', 'user_loyalty', 'user_audit_log', 'surveys');
          " 2>/dev/null | tr -d ' \n')

          REQUIRED_TABLES="users,coupons,user_coupons,tiers,user_loyalty,user_audit_log,surveys"
          MISSING_TABLES=""
          for table in $(echo "$REQUIRED_TABLES" | tr ',' ' '); do
            if ! echo "$TABLES_CHECK" | grep -q "$table"; then
              MISSING_TABLES="$MISSING_TABLES $table"
            fi
          done

          if [ -n "$MISSING_TABLES" ]; then
            echo "❌ Missing tables:$MISSING_TABLES"
            exit 1
          else
            echo "✅ All critical tables exist"
          fi

          # Verify critical stored procedures exist
          echo ""
          echo "📋 Verifying critical stored procedures..."
          FUNCTIONS_CHECK=$(docker compose exec -T postgres psql -U loyalty -d loyalty_db -t -c "
            SELECT string_agg(proname, ',')
            FROM pg_proc p
            JOIN pg_namespace n ON p.pronamespace = n.oid
            WHERE n.nspname = 'public'
            AND proname IN (
              'assign_coupon_to_user',
              'redeem_coupon',
              'award_points',
              'recalculate_user_tier_by_nights'
            );
          " 2>/dev/null | tr -d ' \n')

          REQUIRED_FUNCTIONS="assign_coupon_to_user,redeem_coupon,award_points,recalculate_user_tier_by_nights"
          MISSING_FUNCTIONS=""
          for func in $(echo "$REQUIRED_FUNCTIONS" | tr ',' ' '); do
            if ! echo "$FUNCTIONS_CHECK" | grep -q "$func"; then
              MISSING_FUNCTIONS="$MISSING_FUNCTIONS $func"
            fi
          done

          if [ -n "$MISSING_FUNCTIONS" ]; then
            echo "❌ Missing stored procedures:$MISSING_FUNCTIONS"
            echo "⚠️ Database drift detected - stored procedures missing"
            exit 1
          else
            echo "✅ All critical stored procedures exist"
          fi

          # Verify critical triggers exist
          echo ""
          echo "📋 Verifying critical triggers..."
          TRIGGERS_CHECK=$(docker compose exec -T postgres psql -U loyalty -d loyalty_db -t -c "
            SELECT string_agg(tgname, ',')
            FROM pg_trigger t
            JOIN pg_class c ON t.tgrelid = c.oid
            WHERE NOT t.tgisinternal
            AND tgname IN ('trigger_update_user_tier_on_nights', 'set_membership_id_trigger', 'audit_user_changes');
          " 2>/dev/null | tr -d ' \n')

          REQUIRED_TRIGGERS="trigger_update_user_tier_on_nights,set_membership_id_trigger,audit_user_changes"
          MISSING_TRIGGERS=""
          for trigger in $(echo "$REQUIRED_TRIGGERS" | tr ',' ' '); do
            if ! echo "$TRIGGERS_CHECK" | grep -q "$trigger"; then
              MISSING_TRIGGERS="$MISSING_TRIGGERS $trigger"
            fi
          done

          if [ -n "$MISSING_TRIGGERS" ]; then
            echo "⚠️ Missing triggers:$MISSING_TRIGGERS (may be optional)"
          else
            echo "✅ All critical triggers exist"
          fi

          echo "✅ Database schema verification completed"

          # Rollback safety check
          if docker compose exec -T backend test -f "/app/scripts/migration-rollback-safety.sh"; then
            echo "🔍 Running rollback safety check..."
            docker compose exec -T backend bash -c "cd /app && ./scripts/migration-rollback-safety.sh check" && echo "✅ Rollback safety OK" || echo "⚠️ Rollback safety check failed"
          fi

      - name: "🏥 Health checks & validation (service readiness)"
        run: |
          echo "🏥 Running comprehensive health checks with enhanced logging..."
          
          DEPLOY_DIR="${DEPLOY_PATH:-/home/nut/loyalty-app-production}"
          cd "$DEPLOY_DIR"
          
          # Enhanced logging function with container inspection
          enhanced_health_check() {
            local service=$1
            local url=$2
            local max_attempts=15
            local attempt=0
            
            echo "🔍 Starting health check for $service at $url"
            
            # First, check container status
            echo "📦 Checking container status..."
            docker compose ps
            
            # Show container logs for startup diagnostics
            echo "📜 Recent startup logs from containers:"
            echo "=== Backend Logs (last 50 lines) ==="
            docker compose logs --tail=50 backend 2>/dev/null || echo "No backend logs available"
            
            echo "\n=== Frontend Logs (last 20 lines) ==="
            docker compose logs --tail=20 frontend 2>/dev/null || echo "No frontend logs available"
            
            echo "\n=== Database Logs (last 10 lines) ==="
            docker compose logs --tail=10 postgres 2>/dev/null || echo "No database logs available"
            
            # Check if containers are actually running
            if ! docker compose ps | grep -q "Up"; then
              echo "❌ Critical: No containers are running!"
              echo "🔴 Container status details:"
              docker compose ps -a
              return 1
            fi
            
            # Enhanced health checking with detailed diagnostics
            while [ $attempt -lt $max_attempts ]; do
              echo "🔄 Health Check Attempt $((attempt + 1))/$max_attempts for $service"
              
              # Test with verbose curl output for diagnostics
              if curl -f -s --max-time 3 "$url" > /dev/null 2>&1; then
                echo "✅ $service is healthy and responding"
                
                # Additional validation - get response details
                response=$(curl -s --max-time 3 "$url" 2>/dev/null || echo "No response body")
                echo "📄 Response preview: ${response:0:200}..."
                return 0
              else
                # Detailed failure diagnostics
                http_code=$(curl -s -o /dev/null -w "%{http_code}" --max-time 3 "$url" 2>/dev/null || echo "000")
                echo "⚠️ $service not ready (HTTP: $http_code)"
                
                # Every 3rd attempt, show detailed diagnostics
                if [ $((attempt % 3)) -eq 0 ] && [ $attempt -gt 0 ]; then
                  echo "🔍 Diagnostic info (attempt $((attempt + 1))):"
                  echo "  - Port check:" $(nc -z localhost 4001 && echo "Port 4001 open" || echo "Port 4001 closed")
                  echo "  - Backend container:" $(docker compose exec -T backend echo "responsive" 2>/dev/null || echo "not responding")
                  echo "  - Recent backend errors:"
                  docker compose logs --tail=5 backend 2>/dev/null | grep -i "error\|fail\|exception" | tail -3 || echo "    No recent errors found"
                fi
              fi
              
              attempt=$((attempt + 1))
              [ $attempt -lt $max_attempts ] && sleep 2
            done
            
            echo "❌ $service health check failed after $max_attempts attempts"
            
            # Final diagnostic dump on failure
            echo "🔴 FAILURE DIAGNOSTICS:"
            echo "=== Container Status ==="
            docker compose ps -a
            
            echo "\n=== Network Status ==="
            docker compose exec -T backend cat /etc/hosts 2>/dev/null | head -5 || echo "Cannot access container network info"
            
            echo "\n=== Application Ports ==="
            docker compose exec -T backend netstat -tlnp 2>/dev/null | grep -E ":3000|:4001" || echo "Cannot check application ports"
            
            echo "\n=== Final Backend Logs ==="
            docker compose logs --tail=30 backend
            
            echo "\n=== System Resources ==="
            echo "Memory:" $(free -h | head -2 | tail -1)
            echo "Disk:" $(df -h / | tail -1)
            
            return 1
          }
          
          # Run enhanced health checks
          enhanced_health_check "Application" "http://localhost:4001/api/health"
          
          # If backend is healthy, check frontend
          echo "\n🌐 Checking frontend..."
          if curl -f -s --max-time 3 "http://localhost:4001/" > /dev/null 2>&1; then
            echo "✅ Frontend is responding"
          else
            echo "⚠️ Frontend not responding (non-critical for deployment)"
            echo "📄 Frontend logs:"
            docker compose logs --tail=10 frontend
          fi
          
          echo "✅ Health checks completed successfully"

  # =============================================================================
  # PHASE 4: POST-DEPLOYMENT MONITORING & CLEANUP
  # =============================================================================
  
  post-deployment:
    name: "📊 Post-Deployment"
    runs-on: self-hosted
    needs: production-deployment
    if: always() && (needs.production-deployment.result == 'success' || needs.production-deployment.result == 'failure')
    timeout-minutes: 3
    continue-on-error: true  # Don't block pipeline completion on post-deployment issues
    
    steps:
      - name: "🔍 Pre-deployment health check"
        timeout-minutes: 1
        run: |
          echo "🔍 Checking runner status..."
          echo "Runner: $(hostname)"
          echo "Load: $(uptime)"
          echo "Memory: $(free -h 2>/dev/null || vm_stat | head -10)"
          echo "Disk: $(df -h / | tail -1)"
          echo "✅ Runner health check passed"

      - name: "⚠️ Automatic Rollback on Failure"
        if: failure()
        timeout-minutes: 3
        run: |
          echo "⚠️ Deployment failed - initiating automatic rollback..."

          DEPLOY_DIR="${DEPLOY_PATH:-/home/nut/loyalty-app-production}"
          cd "$DEPLOY_DIR"

          # Find previous working images (excluding current SHA)
          CURRENT_SHA="${{ github.sha }}"
          echo "🔍 Looking for previous working images (excluding $CURRENT_SHA)..."

          PREV_BACKEND=$(docker images loyalty-app-backend --format "{{.Tag}}" | grep -v "latest" | grep -v "$CURRENT_SHA" | head -1)
          PREV_FRONTEND=$(docker images loyalty-app-frontend --format "{{.Tag}}" | grep -v "latest" | grep -v "$CURRENT_SHA" | head -1)

          if [ -n "$PREV_BACKEND" ] && [ -n "$PREV_FRONTEND" ]; then
            echo "📦 Rolling back to previous images:"
            echo "  Backend: loyalty-app-backend:$PREV_BACKEND"
            echo "  Frontend: loyalty-app-frontend:$PREV_FRONTEND"

            # Export previous image tags
            export BACKEND_IMAGE="loyalty-app-backend:$PREV_BACKEND"
            export FRONTEND_IMAGE="loyalty-app-frontend:$PREV_FRONTEND"

            # Perform rollback with same hot-swap strategy
            echo "🔄 Rolling back backend..."
            docker compose -f docker-compose.yml -f docker-compose.prod.yml \
              up -d --no-build --no-deps backend

            sleep 10

            echo "🔄 Rolling back frontend..."
            docker compose -f docker-compose.yml -f docker-compose.prod.yml \
              up -d --no-build --no-deps frontend

            sleep 10

            # Verify rollback
            echo "🏥 Verifying rollback health..."
            if timeout 30s bash -c 'until curl -f -s http://localhost:4001/api/health >/dev/null 2>&1; do sleep 2; done'; then
              echo "✅ Rollback successful - service health check passed"
              echo "📊 Rollback summary:"
              echo "  Rolled back from: $CURRENT_SHA"
              echo "  Rolled back to: $PREV_BACKEND"
              docker compose ps
            else
              echo "❌ Rollback health check failed"
              echo "📄 Backend logs after rollback:"
              docker compose logs --tail=20 backend
            fi
          else
            echo "⚠️ No previous images found for rollback"
            echo "Available backend images:"
            docker images loyalty-app-backend
            echo "Available frontend images:"
            docker images loyalty-app-frontend
          fi

          echo "🔴 Deployment failed and rollback attempted"
          echo "Please review the logs above for details"

      - name: "📊 Deployment summary (reporting only)"
        timeout-minutes: 1
        run: |
          echo "==============================================="
          echo "📊 DEPLOYMENT SUMMARY"
          echo "==============================================="
          echo "Repository: ${{ github.repository }}"
          echo "Branch: ${{ github.ref_name }}"
          echo "Commit: ${{ github.sha }}"
          echo "Actor: ${{ github.actor }}"
          echo "Timestamp: $(date -Iseconds)"
          echo ""
          echo "🎯 Job Results:"
          echo "Validate & Test: ${{ needs.validate-and-test.result }}"
          echo "E2E Tests: ${{ needs.e2e-tests.result || 'skipped' }}"
          echo "Build Validation: ${{ needs.build-validation.result }}"
          echo "Production Deployment: ${{ needs.production-deployment.result }}"
          echo ""
          echo "📈 Metrics:"
          echo "Test Coverage: ${{ needs.validate-and-test.outputs.coverage-percent }}%"
          echo ""
          echo "🌐 Service Endpoints:"
          echo "Application: https://loyalty.saichon.com"
          echo "Health Check: https://loyalty.saichon.com/api/health"
          echo "==============================================="
      
      - name: "🌐 Service health validation"
        timeout-minutes: 1
        run: |
          echo "🌐 Testing deployed services..."
          
          # Test health endpoint with timeout and retry
          for i in {1..3}; do
            if curl -s --max-time 10 "https://loyalty.saichon.com/api/health" | grep -q "ok\|healthy\|success" 2>/dev/null; then
              echo "✅ Service health check passed (attempt $i)"
              break
            else
              echo "⚠️ Health check failed (attempt $i/3)"
              [ $i -eq 3 ] && echo "🚨 All health checks failed - service may be unhealthy"
              sleep 5
            fi
          done
      
      - name: "🧹 Smart cleanup (non-blocking)"
        timeout-minutes: 1
        continue-on-error: true
        run: |
          echo "🧹 Running smart cleanup with strict timeouts..."
          
          # Set strict timeout for all operations
          set +e  # Don't exit on command failures in cleanup
          
          # Check disk usage with timeout
          DISK_USAGE=$(timeout 10s df / | tail -1 | awk '{print $5}' | sed 's/%//' 2>/dev/null || echo "unknown")
          echo "💾 Disk usage: ${DISK_USAGE}%"
          
          # Clean up old TEST containers only (NEVER prune all - protects dev containers)
          echo "🐳 Cleaning old test containers..."
          # Only remove E2E test containers, never use 'docker container prune' which could delete dev containers
          for container in loyalty_postgres_e2e loyalty_redis_e2e loyalty_backend_e2e loyalty_frontend_e2e \
                          loyalty_postgres_unit loyalty_redis_unit loyalty_postgres_integration loyalty_redis_integration; do
            docker rm -f "$container" 2>/dev/null || true
          done
          echo "✅ Test containers cleaned (dev/prod containers preserved)"
          
          # Clean up old images with timeout (keep last 3 versions)
          echo "🖼️ Cleaning old images..."
          timeout 20s docker image prune -f --filter "until=72h" 2>/dev/null || echo "⚠️ Image cleanup timed out"
          
          # Clean up build cache with timeout
          echo "🧹 Cleaning build cache..."
          timeout 10s docker builder prune -f --filter "until=24h" 2>/dev/null || echo "⚠️ Build cache cleanup timed out"
          
          echo "✅ Cleanup completed (may have warnings, non-blocking)"

      - name: "📋 Post-deployment summary"
        timeout-minutes: 1
        continue-on-error: true
        env:
          DEPLOY_RESULT: ${{ needs.production-deployment.result }}
        run: |
          echo "==============================================="
          echo "📋 DEPLOYMENT RESULT"
          echo "==============================================="

          if [ "$DEPLOY_RESULT" = "success" ]; then
            HEALTH_CODE=$(curl -s -o /dev/null -w '%{http_code}' https://loyalty.saichon.com/api/health 2>/dev/null || echo 'timeout')
            echo "🎉 Deployment Status: ✅ SUCCESS"
            echo "├── 🌐 Frontend: https://loyalty.saichon.com"
            echo "├── 🔗 Backend: https://loyalty.saichon.com/api"
            echo "├── 📊 Health check: $HEALTH_CODE"
            echo "└── 🕒 Completed at: $(date)"
          else
            echo "❌ Deployment Status: FAILED"
            echo "├── Result: $DEPLOY_RESULT"
            echo "├── Commit: ${{ github.sha }}"
            echo "├── Branch: ${{ github.ref_name }}"
            echo "└── 🕒 Failed at: $(date)"
            echo ""
            echo "🔍 Check the 'Production Deployment' job logs for details"
          fi

          echo "==============================================="
          echo "🎊 Post-deployment job completed - pipeline will not get stuck"

      - name: "🧹 Cleanup production env file"
        if: always()
        run: |
          DEPLOY_DIR="${DEPLOY_PATH:-/home/nut/loyalty-app-production}"
          if [ -d "$DEPLOY_DIR" ]; then
            rm -f "$DEPLOY_DIR/.env" || true
            echo "✅ Cleaned up .env file in $DEPLOY_DIR"
          else
            echo "⚠️ Deploy directory $DEPLOY_DIR does not exist - skipping cleanup"
          fi

  # =============================================================================
  # UNIFIED TEST REPORT GENERATION (Merges all test types)
  # =============================================================================

  generate-test-reports:
    name: "📊 Generate Unified Test Reports"
    runs-on: ubuntu-latest
    needs: [validate-and-test, e2e-tests]
    # Only generate reports if at least one test job succeeded (has actual results)
    # Skip if both jobs failed/cancelled/skipped to avoid publishing empty "?" results
    if: |
      github.ref == 'refs/heads/main' &&
      (needs.validate-and-test.result == 'success' || needs.e2e-tests.result == 'success')

    # Prevent multiple concurrent report generations to avoid artifact conflicts
    concurrency:
      group: "test-reports-${{ github.repository }}"
      cancel-in-progress: true

    steps:
      - name: "📥 Checkout code"
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6
        with:
          fetch-depth: 1

      - name: "⚡ Setup Node.js"
        uses: actions/setup-node@395ad3262231945c25e8478fd5baf05154b1d79f # v6
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: "📥 Download unit test results"
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7.0.0
        with:
          name: allure-results-unit-${{ github.run_id }}
          path: allure-results-unit/
        continue-on-error: true

      - name: "📥 Download integration test results"
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7.0.0
        with:
          name: allure-results-integration-${{ github.run_id }}
          path: allure-results-integration/
        continue-on-error: true

      - name: "📥 Download E2E test results"
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7.0.0
        with:
          name: allure-results-e2e-${{ github.run_id }}
          path: allure-results-e2e/
        continue-on-error: true

      - name: "📥 Download coverage report"
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7.0.0
        with:
          name: coverage-report-${{ github.run_id }}
          path: coverage-report/
        continue-on-error: true

      - name: "🔀 Merge all test results"
        run: |
          # Verify Node.js version
          echo "🔍 Node.js: $(node --version) | npm: $(npm --version)"
          NODE_MAJOR=$(node --version | cut -d'.' -f1 | tr -d 'v')
          if [ "$NODE_MAJOR" -lt "${{ env.NODE_VERSION }}" ]; then
            echo "❌ Node.js version mismatch! Expected ${{ env.NODE_VERSION }}.x, got $(node --version)"
            exit 1
          fi

          echo "🔀 Merging test results from all test types..."

          # Create combined results directory
          mkdir -p backend/allure-results-combined

          # Copy unit test results
          if [ -d "allure-results-unit" ] && [ "$(ls -A allure-results-unit 2>/dev/null)" ]; then
            echo "✅ Copying unit test results..."
            cp -r allure-results-unit/* backend/allure-results-combined/ 2>/dev/null || true
            UNIT_COUNT=$(ls -1 allure-results-unit | wc -l)
            echo "   📊 Unit test files: $UNIT_COUNT"
          else
            echo "⚠️ No unit test results found"
          fi

          # Copy integration test results
          if [ -d "allure-results-integration" ] && [ "$(ls -A allure-results-integration 2>/dev/null)" ]; then
            echo "✅ Copying integration test results..."
            cp -r allure-results-integration/* backend/allure-results-combined/ 2>/dev/null || true
            INTEGRATION_COUNT=$(ls -1 allure-results-integration | wc -l)
            echo "   📊 Integration test files: $INTEGRATION_COUNT"
          else
            echo "⚠️ No integration test results found"
          fi

          # Copy E2E test results
          if [ -d "allure-results-e2e" ] && [ "$(ls -A allure-results-e2e 2>/dev/null)" ]; then
            echo "✅ Copying E2E test results..."
            cp -r allure-results-e2e/* backend/allure-results-combined/ 2>/dev/null || true
            E2E_COUNT=$(ls -1 allure-results-e2e | wc -l)
            echo "   📊 E2E test files: $E2E_COUNT"
          else
            echo "⚠️ No E2E test results found"
          fi

          # Count total merged results
          TOTAL_COUNT=$(ls -1 backend/allure-results-combined 2>/dev/null | wc -l)
          echo "📊 Total merged test result files: $TOTAL_COUNT"

          if [ "$TOTAL_COUNT" -eq 0 ]; then
            echo "⚠️ No test results to merge - creating placeholder"
            mkdir -p backend/allure-results-combined
            echo '{"name":"No tests found","status":"broken"}' > backend/allure-results-combined/placeholder.json
          fi

      - name: "☕ Setup Java for Allure"
        uses: actions/setup-java@c1e323688fd81a25caa38c78aa6df2d33d3e20d9 # v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: "📊 Install Allure CLI & generate unified test report"
        run: |
          cd backend

          # Install allure-commandline from package.json (integrity verified via package-lock.json)
          npm ci --prefer-offline --no-audit --ignore-scripts

          echo "📊 Generating unified Allure report from merged results..."

          if [ -d "allure-results-combined" ] && [ "$(ls -A allure-results-combined)" ]; then
            echo "✅ Generating Allure report..."
            npx allure generate allure-results-combined --clean -o allure-report
            echo "✅ Unified Allure report generated successfully"
            echo "📊 Report includes: Unit Tests + Integration Tests + E2E Tests (Playwright)"
          else
            echo "⚠️ No test results found, creating placeholder report"
            mkdir -p allure-report
            cat > allure-report/index.html <<'EOF'
          <!DOCTYPE html>
          <html>
          <head><title>No Test Results</title></head>
          <body><h1>No test results available for this run</h1></body>
          </html>
          EOF
          fi

      - name: "📦 Create redirect page & prepare GitHub Pages artifact structure"
        run: |
          echo "🔗 Creating latest redirect page..."
          mkdir -p test-reports-latest

          BRANCH_NAME="${{ github.ref_name }}"
          if [ "$BRANCH_NAME" = "main" ]; then
            BADGE_COLOR="#28a745"; BADGE_TEXT="🚀 PRODUCTION"; ENV_NAME="Production"
          else
            BADGE_COLOR="#17a2b8"; BADGE_TEXT="🧪 DEVELOPMENT"; ENV_NAME="Development"
          fi

          cat > test-reports-latest/index.html <<REDIRECT_EOF
          <!DOCTYPE html>
          <html lang="en">
          <head>
              <meta charset="UTF-8">
              <meta name="viewport" content="width=device-width, initial-scale=1.0">
              <title>[$ENV_NAME] Test Report #${{ github.run_number }}</title>
              <style>
                  body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; display: flex; justify-content: center; align-items: center; min-height: 100vh; margin: 0; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; }
                  .container { text-align: center; padding: 2rem; background: rgba(255,255,255,0.1); border-radius: 10px; backdrop-filter: blur(10px); max-width: 500px; }
                  .badge { display: inline-block; padding: 0.5rem 1rem; background: $BADGE_COLOR; border-radius: 20px; font-weight: bold; margin-bottom: 1rem; }
                  .links { display: flex; gap: 1rem; justify-content: center; margin: 1.5rem 0; flex-wrap: wrap; }
                  .link-btn { display: inline-block; padding: 0.75rem 1.5rem; background: rgba(255,255,255,0.2); border-radius: 8px; color: white; text-decoration: none; transition: background 0.2s; }
                  .link-btn:hover { background: rgba(255,255,255,0.3); }
                  .link-btn.primary { background: rgba(255,255,255,0.3); }
                  .meta { font-size: 0.9rem; opacity: 0.8; margin-top: 1rem; }
              </style>
          </head>
          <body>
              <div class="container">
                  <div class="badge">$BADGE_TEXT</div>
                  <h2>Test Reports #${{ github.run_number }}</h2>
                  <div class="links">
                      <a href="../${{ github.run_number }}/" class="link-btn primary">📊 Allure Report</a>
                      <a href="../${{ github.run_number }}/coverage/lcov-report/" class="link-btn">📈 Coverage Report</a>
                  </div>
                  <div class="meta">Branch: $BRANCH_NAME | Commit: ${{ github.sha }}</div>
              </div>
          </body>
          </html>
          REDIRECT_EOF
          echo "✅ Redirect page created for $ENV_NAME"

          echo ""
          echo "📦 Preparing GitHub Pages artifact..."
          mkdir -p pages-artifact/test-reports

          if [ -d "backend/allure-report" ]; then
            cp -r backend/allure-report pages-artifact/test-reports/${{ github.run_number }}
            echo "✅ Allure report copied"
          else
            mkdir -p pages-artifact/test-reports/${{ github.run_number }}
            echo "<h1>No test results</h1>" > pages-artifact/test-reports/${{ github.run_number }}/index.html
          fi

          # Include coverage report if available
          if [ -d "coverage-report" ] && [ "$(ls -A coverage-report 2>/dev/null)" ]; then
            mkdir -p pages-artifact/test-reports/${{ github.run_number }}/coverage
            cp -r coverage-report/* pages-artifact/test-reports/${{ github.run_number }}/coverage/
            echo "✅ Coverage report copied"
          else
            echo "⚠️ No coverage report found"
          fi

          mkdir -p pages-artifact/test-reports/latest
          cp -r test-reports-latest/* pages-artifact/test-reports/latest/

          cat > pages-artifact/index.html <<'ROOT_EOF'
          <!DOCTYPE html>
          <html><head><meta http-equiv="refresh" content="0; url=./test-reports/latest/"><title>Test Reports</title></head>
          <body><p>Redirecting to <a href="./test-reports/latest/">latest report</a>...</p></body></html>
          ROOT_EOF

          echo "✅ Artifact prepared ($(du -sh pages-artifact/ | cut -f1))"

      - name: "📤 Upload GitHub Pages artifact"
        uses: actions/upload-pages-artifact@7b1f4a764d45c48632c6b24a0339c27f5614fb0b # v4
        with:
          path: pages-artifact
          retention-days: 90
          # Note: artifact name is always 'github-pages' for this action

  # =============================================================================
  # GITHUB PAGES DEPLOYMENT (Deploys unified test reports)
  # Uses concurrency to prevent duplicate artifact conflicts
  # =============================================================================

  deploy-github-pages:
    name: "📄 Deploy to GitHub Pages"
    runs-on: ubuntu-latest
    needs: [generate-test-reports]
    # Only deploy if report generation succeeded (has valid test results)
    if: github.ref == 'refs/heads/main' && needs.generate-test-reports.result == 'success'

    permissions:
      pages: write
      id-token: write
      contents: read

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    # Strict concurrency - only one pages deployment at a time
    # This prevents the "Multiple artifacts named github-pages" error
    concurrency:
      group: "pages-deploy-${{ github.repository }}"
      cancel-in-progress: true

    steps:
      - name: "⏳ Wait for artifact to be ready"
        run: |
          echo "⏳ Waiting for artifact processing..."
          sleep 5
          echo "✅ Proceeding with deployment"

      - name: "🚀 Deploy to GitHub Pages"
        id: deployment
        uses: actions/deploy-pages@d6db90164ac5ed86f2b6aed7e0febac5b3c0c03e # v4
        with:
          timeout: 600000
          error_count: 10
          reporting_interval: 5000
        # Uses default artifact_name: github-pages

      - name: "✅ Deployment complete"
        run: |
          echo "📄 GitHub Pages deployed successfully!"
          echo "🔗 URL: ${{ steps.deployment.outputs.page_url }}"
          echo "📊 Test Reports: ${{ steps.deployment.outputs.page_url }}test-reports/latest/"

# =============================================================================
# PIPELINE SUMMARY:
#
# 📊 PERFORMANCE OPTIMIZATIONS:
# - Parallel job execution with isolated environments (prevents conflicts)
# - Port isolation strategy (Unit: 5438/6383, Integration: 5437/6382, E2E: 5436/6381/4202/3201, Dev: 5435/6380)
# - Container isolation with unique project names and containers
# - Intelligent caching (npm, Docker BuildKit)
# - Conditional jobs (E2E only on main/PR to main)
# - Smart dependency installation
# - Shallow git clones
# - Optimized Docker builds
#
# 🔒 SECURITY & QUALITY:
# - ESLint security rules
# - npm audit
# - Custom security validation
# - Test integrity validation (prevents test bypassing)
# - TypeScript type checking
# - Unit tests (parallel)
# - Integration tests (parallel)
# - E2E tests (conditional)
# - Database schema tests
# - OAuth health validation (pre & post deployment)
# - Database migration validation & rollback safety
# - OAuth E2E validation tests
#
# ⚡ ESTIMATED TIMES:
# - Phase 0 (Workspace Prep): 30-60 seconds
# - Phase 1A (Security): 3-4 minutes (parallel)
# - Phase 1B (Unit Tests): 3-4 minutes (parallel)
# - Phase 1C (Integration Tests): 4-5 minutes (parallel)
# - Phase 1D (E2E Tests): 8-12 minutes (conditional, after workspace prep)
# - Phase 2 (Build): 2-3 minutes (only on main, after all tests)
# - Phase 3 (Deploy): 3-5 minutes (only on main, production)
# - Total: 11-15 minutes (improved from 13-18 minutes due to parallel tests)
#
# 🎯 IMPROVEMENTS:
# - 100% environment isolation (prevents race conditions)
# - PARALLEL unit and integration tests (saves 2-3 minutes)
# - Comprehensive test coverage with parallel security analysis
# - Enhanced security validation
# - Better error handling and reporting
# - Smart caching for dependencies
# - Conditional execution to save resources
# - Reliable testing environments with port separation
# - Independent test job outputs for better visibility
# =============================================================================
