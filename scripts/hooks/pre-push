#!/bin/bash

# Lightweight Pre-Push Hook - ESLint and TypeCheck Only
# For faster pushes while maintaining code quality
# Tests and builds are handled by CI/CD pipeline

set -e  # Exit on any error

echo "🚀 Pre-Push Quality Check (Lightweight)..."
echo "================================================"

# Color codes for output
RED='\033[0;31m'
GREEN='\033[0;32m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

# Check if we're in the project root
if [ ! -f "package.json" ] || [ ! -d "frontend" ]; then
    echo -e "${RED}[ERROR]${NC} Must be run from project root directory"
    exit 1
fi

# 1. Frontend TypeScript Check
echo -e "${BLUE}[1/3]${NC} Frontend TypeScript check..."
if cd frontend && npm run typecheck; then
    echo -e "${GREEN}✅ Frontend TypeScript passed${NC}"
else
    echo -e "${RED}❌ Frontend TypeScript check failed${NC}"
    exit 1
fi
cd ..

# 2. Frontend ESLint
echo -e "${BLUE}[2/3]${NC} Frontend ESLint..."
if cd frontend && npm run lint; then
    echo -e "${GREEN}✅ Frontend ESLint passed${NC}"
else
    echo -e "${RED}❌ Frontend ESLint failed${NC}"
    exit 1
fi
cd ..

# 3. Translation Key Check
echo -e "${BLUE}[3/3]${NC} Translation key check..."
if cd frontend && npm run check:translations; then
    echo -e "${GREEN}✅ Translation keys validated${NC}"
else
    echo -e "${RED}❌ Translation check failed${NC}"
    exit 1
fi
cd ..

# Summary
echo ""
echo "================================================"
echo -e "${GREEN}🎉 All pre-push checks passed!${NC}"
echo -e "${GREEN}✅ TypeScript compilation successful${NC}"
echo -e "${GREEN}✅ ESLint checks passed${NC}"
echo -e "${GREEN}✅ Translation keys validated${NC}"
echo ""
echo -e "${BLUE}🚀 Push to remote is approved!${NC}"
echo "================================================"

exit 0
